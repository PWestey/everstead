# PHASE 10B-2 — GOLD CORE EXTRACTION EXECUTION

Status: **PREIMAGE QA READY / PRODUCTION EXTRACTION PENDING**

Authoritative contract: `docs/PHASE_10B2_GOLD_CORE_EXTRACTION_CONTRACT.md` at reviewed commit `fc7553c6960080c596d0b4f6da3dd262dff2e31d`.

## Completed before production work

- The production preimage remains exact SHA-256 `717160cdddc5fa540532cdebd29f30d127ded2f761edd677684a2609fde9a4ed`, 18,916,682 bytes.
- The 32-trace predecessor authority remains exact SHA-256 `74feec74ff6f9efe07ca47cb2b0fcaaca8ab80dab71961fd2f1625f270209136`, 557,500 bytes.
- The independent seven-method arithmetic reference, exact 96-vector scenario authority, exact 400-row registry, reversible read-only production probe, exact 224-path successor map, successor runner, and exact 228-row browser topology are additive.
- The repaired reference/trace boundary rejects sparse, accessor-backed, extra-key, symbol-key, custom-prototype, or descriptor-invalid authority values. Exact comparison includes prototypes, own-key order, density, and descriptors recursively.
- The focused gate now binds literal arithmetic inputs to independently planned operation-specific calls, captures actual candidate call records in separate reversible realms, and executes exact realm-native refusal groups covering structure, field order, first-invalid paths, relationships, boundaries, signed zero, and arithmetic overflow.
- Nineteen real isolated persistence fixtures are allocated across the exact 16 persistence rows. They include maximum-valid state, capped 24-hour accrual, fractional pending Gold, migrations, recovery positions, reset, settlement boundaries, write/cleanup faults, and twelve-slot export faults. Candidate comparison preserves descriptors, types, Float64 bits, state, UI, raw values, revisions, every protected slot, and logs without JSON normalization.
- The verifier snapshots every owned path—including absence sentinels for the not-yet-created result, manifest, and checksum files—and the literal 224-file predecessor union. The seal builder verifies that its explicit package commit owns the complete current 13-file executable aggregate before it may write evidence metadata.
- Normal preimage verification reports PASS/PENDING/FAIL separately. Release mode refuses the unchanged artifact.

## Preimage acceptance state

| Gate | Honest preimage result |
| --- | --- |
| Focused CLI | 206 PASS / 194 PENDING / 0 FAIL, exact 400 |
| Live dashboard | 84 PASS / 144 candidate rows PENDING / 0 FAIL, exact 228, on both a fresh pass and click rerun; blank fatal output, 228 unique row IDs, zero retained realm frames, and zero browser warning/error console entries |
| Phase 10B semantic successor | 620 PASS / 4 candidate statics PENDING / 0 FAIL, exact 624, twice |
| Byte successor | 224/224 on unchanged preimage, twice; candidate supersession PENDING |
| Phase 10B checksums | 22/22 on unchanged preimage, twice |
| Read-only builder | `PREIMAGE_READY`, 13 executable files, writes 0, twice |

Candidate-only rows cover the private surface, startup guard, both candidate trace-observation lanes (wrapper/call records and direct kernel), exact refusals, all base-versus-candidate persistence-equivalence rows, and all 48 mutation rows/51 isolated subcases. They cannot become PASS until the authorized kernel and six wrapper delegations exist. Frozen predecessor returns and the independent reference remain PASS authorities; they are not mislabeled as candidate observations.

The materially strengthened preimage browser package was run in the in-app Chromium browser after all three QA repair workstreams stabilized. Its deferred fresh pass and click rerun each rendered exact 228 unique rows with 84 PASS, 144 explicitly PENDING, zero FAIL, blank fatal output, zero retained realm frames, and zero browser warning/error console entries. This is current-package preimage evidence only; it is not the required post-extraction 228/228 candidate acceptance result.

## Next authorized step

Implement one production-only commit containing exactly the private kernel insertion immediately before `const RUNTIME_INPUT=` and the six wrapper-body delegations allowed by the contract. Then hardcode the reviewed candidate SHA and byte length in the probe, complete the candidate mutation transforms and four revised Phase 10B successor statics, run all focused/successor/inherited/live gates twice, obtain two independent reviews, and only then create the result/manifest/checksum evidence seal.

No result document, manifest, or Phase 10B-2 checksum file is created at this preimage stage.
