# EVERSTEAD — PHASE 6 RESULT

## Candidate

- Base: sealed Phase 5 package `4c2f27b02d177afbf08898eee68a4b552b4c3d8c`.
- Production tip: `689d43a4f0b0d22507e66b158f4a1f23a8672fee`.
- Production artifact SHA-256: `63182db2f73d9d5e7d723c4e6ce1fea520d7803c6314ff1307180ad9b1d3635f`.
- Production artifact bytes: `18,627,378`.

## Delivered behavior

- Schema 7 and an exact ninth protected slot (`pre-v7`) with deterministic schema 0–6 migration, receipt-bound ancestry, safe export/reset, and fail-closed recovery.
- Global Companion Mastery derived only from Tower first clears and claimed idle entitlement, applied once in the Companion Power pipeline.
- Ten-stage Companion Campaign using Total Companion Roster Power, Gold efficiency/spend, targeted EXP/shards, first-clear/replay ledgers, and atomic deterministic receipts.
- Fifty-floor Companion Tower with sequential no-cost clears, broad Companion EXP, Mastery, deterministic fifth-floor shards, and no Gold/Prosperity faucet.
- Independent 24-hour Tower idle lane with chronological floor segments, hourly EXP/Mastery, deterministic random shards, seven-miss pity, exact claim receipts, and double-claim refusal.
- Three active Adventure routes, Companion walk presentation, explicit Tower hourly rates/next-clear preview, durable last-clear/last-claim summaries, and actual reward-recipient feedback.
- Historical Phase 5 schema-6 fresh, migration, ordinary-current, and safe-reset staging completes safely before Phase 6 migration; reserved collisions and malformed/foreign/racing checkpoint ancestry refuse with zero writes.

## Gate evidence

- Phase 6 CLI: `258/258` on the production candidate before package generation.
- Phase 5 semantic successor: `515/515` with eight itemized Phase 6 replacements.
- The first live pass reached `616/620`; all four failures were the two Tower history-copy requirements repeated at both mobile sizes. Production now states `Floor N cleared` and shows aggregate idle EXP alongside per-recipient EXP.
- Two final live Chromium runs on the frozen package tip each reached `620/620`, with a blank fatal field, zero failed rows, and zero warning/error console entries across both configured mobile sizes.

## Intentional fail-closed boundary

A lone evolved schema-6 pre-v7 checkpoint with both active and schema-7 staging missing remains retained but is not adopted. Recovery requires the exact active predecessor, an authenticated staged schema-7 successor, or deterministic immediate-predecessor reconstruction. This preserves ancestry rather than trusting self-declared evolved bytes.

## Residual risks and deferred validation

- Web Storage has no atomic compare-and-swap; exact ownership rereads, revisions, raw identities, and storage events narrow and detect but cannot eliminate the final same-origin race.
- FNV raw identities bind accidental/foreign divergence but are not cryptographic authentication against a malicious same-origin editor.
- Safari and real-device behavior remain outside the Chromium gate.
- All Phase 10 balancing, Golemore/Might, Relics, automation, audio, events, advanced animation, and Post-V1 systems remain deferred.
