(function(g){'use strict';g.installKitchenUI=function(A){
 let tab='service',selected=0,guestIndex=0,message='';const E=g.EVERSTEAD_KITCHEN,H=g.EVERSTEAD_HOSPITALITY,esc=A.escape;
 const ingredients=r=>r.pantry.map((n,i)=>`<span>${E.ingredients[i]} <b>${n}</b></span>`).join('');
 function render(){const r=E.settle(A.state().restaurantKitchen,A.now());if(!r)return;const d=E.recipes[selected],level=r.levels[selected],bonus=E.bonuses(r),h=A.state().restaurantHospitality,totals=H.totals(h),cast=A.cast(),preview=H.preview(h,selected,cast),guest=cast.find(p=>p.id===h.pending?.guest)||preview.guest;let body='';
  if(!r.tutorial||tab==='guide')body=`<h3>A kitchen that grows with you</h3><p>Collect deliveries in Pantry. Unlock Hearthbread free in Recipes, then cook for waiting customers in Service.</p><p>Each served table pays Gold and 2 Chef Notes. Spend Notes to unlock and improve dishes: each recipe level adds <b>120 Village Gold/hr and 10 Power to each owned Fellow</b>.</p><p>Customers bank up to 12; supply crates bank up to 6. Tables and ingredients never expire. EXP stays yours to spend separately.</p><button class="va-primary" data-k-action="tutorial">OPEN THE KITCHEN</button>`;
  else if(tab==='room-guide'||(tab==='room'||tab==='guests')&&!h.tutorial)body=`<h3>A place your guests remember</h3><p>Each paid table earns 1 Service Seal and 3 Popularity. A favorite dish adds 1 Seal and 2 Popularity. Any unlocked dish is welcome.</p><p>Spend Seals on matching cooking stations. Each station level adds 25 Gold and 1 Popularity to future tables using it, plus permanent +60 Gold/hr and +5 Power per owned Fellow.</p><p>Popularity introduces more guests. Existing tables keep their original rewards; nothing expires.</p><button class="va-primary" data-k-action="hospitality-guide">WELCOME YOUR GUESTS</button>`;
  else if(tab==='room'){const stationLevel=h.levels[selected],price=H.cost(stationLevel);body=`<div class="k-dish-picker"><button data-k-shift="-1" aria-label="Previous station">‹</button><div class="k-dish-art">${['♨','♜','♧','♛'][selected]}</div><button data-k-shift="1" aria-label="Next station">›</button></div><h3>${H.stations[selected]} · Lv. ${stationLevel}/5</h3><p>Improves ${d.name}. +25 Gold per future table per level.<br>Next level also adds +60 Gold/hr and +5 Power per owned Fellow.</p><p>Popularity ${totals.popularity} · Service Seals ${totals.seals}</p><button class="va-primary" data-k-action="station" data-k-id="${selected}" ${stationLevel>=5||totals.seals<price||totals.popularity<H.gates[selected]?'disabled':''}>${totals.popularity<H.gates[selected]?`NEEDS ${H.gates[selected]} POPULARITY`:stationLevel>=5?'STATION MASTERED':`UPGRADE · ${price} SEALS`}</button><button class="va-primary" data-k-tab="guests">OPEN GUESTBOOK</button>`;}
  else if(tab==='guests'){const count=Math.min(cast.length,4+Math.floor(totals.popularity/10)*2),person=cast[guestIndex%count];body=`<div class="k-dish-picker"><button data-k-guest="-1" aria-label="Previous guest">‹</button><img class="k-guest-portrait" src="${esc(person.portrait)}" alt="${esc(person.name)}"><button data-k-guest="1" aria-label="Next guest">›</button></div><h3>${esc(person.name)}</h3><p>Favorite: ${E.recipes[person.favorite].name}<br>${h.visits[person.id]||0} paid visits · Guest ${guestIndex%count+1}/${count}</p><p>${count===cast.length?'All current guests introduced.':`${count} guests introduced. More arrive at ${(Math.floor(totals.popularity/10)+1)*10} Popularity.`}</p><button class="va-primary" data-k-tab="room">BACK TO STATIONS</button>`;}
  else if(tab==='pantry')body=`<h3>Village deliveries</h3><div class="va-stats">${ingredients(r)}</div><p>Each crate: 4 Grain · 3 Vegetables · 2 Herbs · 2 Fish.</p><p>${r.crates} crates waiting. One arrives every 30 minutes.</p><button class="va-primary" data-k-action="supplies" ${r.crates?'':'disabled'}>UNPACK ONE CRATE</button><p>Grow additional ingredients or land culinary fish. Outdoor supplies arrive here automatically.</p><div class="k-outdoors"><button class="va-primary" data-k-outdoors="gardens">GARDENS</button><button class="va-primary" data-k-outdoors="fishing">FISHING</button></div>`;
  else if(tab==='service'&&r.pending){const p=r.pending,meal=E.recipes[p.recipe],hp=h.pending;body=`<div class="k-dish-art">${meal.icon}</div><h3>${meal.name} · served!</h3><p>${hp?esc(guest.name):'Your guest'} enjoyed this level ${p.level} dish. Payment waits until collected.</p><div class="va-reward"><strong>+${p.gold+(hp?.level||0)*25} Gold</strong><span>+2 Chef Notes${hp?` · +${1+Number(hp.favorite)} Seals · +${3+hp.level+2*Number(hp.favorite)} Popularity`:''}</span></div><button class="va-primary va-claim" data-k-action="claim" data-k-id="${p.id}">COLLECT TABLE PAYMENT</button>`;}
  else{
   body=`<div class="k-dish-picker"><button data-k-shift="-1" aria-label="Previous dish">‹</button><div class="k-dish-art">${d.icon}</div><button data-k-shift="1" aria-label="Next dish">›</button></div><h3>${d.name} <small>${level?'Lv. '+level:'Locked'}</small></h3><p>Recipe ${selected+1} of ${E.recipes.length}</p>`;
   if(tab==='recipes'){const cost=E.price(r,selected);body+=`<p>${level?'Improve':'Unlock'} this dish for <b>+120 Village Gold/hr</b> and <b>+10 Power per owned Fellow</b>. Permanent, additive bonuses.</p><p>Dish earnings: ${level*d.gold} → ${Math.min(10,level+1)*d.gold} Gold per table.</p><button class="va-primary" data-k-action="improve" data-k-id="${selected}" ${level===10||r.notes<cost?'disabled':''}>${level===10?'RECIPE MASTERED':(level?'UPGRADE':'UNLOCK')+' · '+(cost?cost+' NOTES':'FREE')}</button>`;}
   else body+=`<p>Ingredients: ${d.cost.map((n,i)=>n?`${n} ${E.ingredients[i]}`:'').filter(Boolean).join(' · ')}</p><p>Serve one of ${r.diners} waiting customers for ${d.gold*level} Gold + 2 Notes.</p><button class="va-primary" data-k-action="cook" data-k-id="${selected}" ${!level||!r.diners||r.pantry.some((n,i)=>n<d.cost[i])?'disabled':''}>COOK & SERVE</button><p>${!level?'Unlock this dish in Recipes.':r.pantry.some((n,i)=>n<d.cost[i])?'Ingredients missing? Visit Pantry or choose another dish.':'Customers arrive every 15 minutes; no rush to serve.'}</p>`;
  }
  if(tab==='service'&&!r.pending&&r.tutorial)body=body.replace(`<p>Recipe ${selected+1} of ${E.recipes.length}</p>`,`<p>${esc(guest.name)} loves ${E.recipes[guest.favorite].name}. ${preview.favorite?'Favorite: +1 extra Seal and +2 Popularity.':'Any dish is welcome.'}</p>`).replace(`${d.gold*level} Gold + 2 Notes`,`${d.gold*level+preview.gold} Gold + 2 Notes`);
  if(tab==='guide'&&r.tutorial)body+='<button class="va-primary" data-k-tab="room-guide">STATIONS & GUESTS GUIDE</button>';
  const legacy=A.state().villageActivities?.facilities.restaurant;
  A.showModal(`<section class="va-sheet kitchen-sheet" style="--va-color:#9d6539"><header class="va-head"><div><small>EVERSTEAD · THE RESTAURANT</small><h2>The Founding Table</h2></div><button class="va-close" data-k-close aria-label="Close Restaurant">×</button></header><div class="va-scene"><img class="k-scene-guest" src="${esc(guest.portrait)}" alt="${esc(guest.name)}"><div><h3>${esc(guest.name)}’s table</h3><p>+${bonus.earnings+totals.earnings} Gold/hr · +${bonus.power+totals.power} Power / Fellow</p></div></div><div class="va-stats"><span>Guests <b>${r.diners}/12</b></span><span>Notes <b>${r.notes}</b></span><span>Popularity <b>${totals.popularity}</b></span></div><div class="va-content">${message?`<p class="va-message" role="status">${esc(message)}</p>`:''}${body}${legacy?.active||legacy?.pending?'<button class="va-primary" data-k-legacy>FINISH PREVIOUS ACTIVITY / CLAIM REWARD</button>':''}</div><footer class="va-footer">${['service','recipes','room','pantry','guide'].map(t=>`<button data-k-tab="${t}" aria-pressed="${tab===t||t==='room'&&tab==='guests'}">${{service:'Service',recipes:'Recipes',room:'Room',pantry:'Pantry',guide:'Guide'}[t]}</button>`).join('')}</footer></section>`);
  const root=document.querySelector('.kitchen-sheet');root.querySelector('[data-k-close]').onclick=A.close;
  root.querySelectorAll('[data-k-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.kTab;message='';render()});
  root.querySelectorAll('[data-k-shift]').forEach(b=>b.onclick=()=>{selected=(selected+Number(b.dataset.kShift)+4)%4;message='';render()});
  root.querySelectorAll('[data-k-guest]').forEach(b=>b.onclick=()=>{const count=Math.min(cast.length,4+Math.floor(totals.popularity/10)*2);guestIndex=(guestIndex+Number(b.dataset.kGuest)+count)%count;render()});
  root.querySelectorAll('[data-k-action]').forEach(b=>b.onclick=()=>{const result=A.act(b.dataset.kAction,Number(b.dataset.kId));message=result.ok?'':result.reason;if(result.ok&&b.dataset.kAction==='tutorial')tab='recipes';if(result.ok&&b.dataset.kAction==='hospitality-guide')tab='room';render()});
  root.querySelector('[data-k-legacy]')?.addEventListener('click',A.legacy);
  root.querySelectorAll('[data-k-outdoors]').forEach(b=>b.onclick=()=>A.outdoors?.(b.dataset.kOutdoors));
 }
 return()=>{tab='service';message='';render()};
};})(globalThis);
