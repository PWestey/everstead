/* Everstead Phase 24C · pure, inactive durable-ladder and Collection primitives v2. */
(function installEversteadPhaseTwentyFourCDurableFoundation(global){
  'use strict';

  const definitions=global.EVERSTEAD_PHASE24C_DEFINITIONS;
  if(!definitions||definitions.version!==1)throw new Error('Phase 24C durable definitions must load first');

  const ROOT_KEY='durableProgression';
  const SUCCESSOR_SCHEMA=14;
  const MIGRATION_ID='migration.schema-13-to-14.phase-24c-foundation.v2';
  const CHECKPOINT_KIND='phase24c.pre-v14-checkpoint.v1';
  const DIRECT_ORIGIN_KIND='phase24c.direct-schema-14-origin.v1';
  const LINEAGE_KINDS=new Set(['direct-schema-14','safe-reset-schema-14','schema-13-to-14-migration']);
  const RECENT_GRANT_LIMIT=16;
  const GRANT_FOLD_BATCH=8;
  const ID_PATTERN=/^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$/;
  const HASH_PATTERN=/^[0-9a-f]{64}$/;
  const RAW_IDENTITY_PATTERN=/^(?:null:0:00000000|fnv1a32:\d+:[0-9a-f]{8})$/;
  const POOL_IDS=new Set(['power','earnings','exp','facility']);
  const CLASSIFICATIONS=new Set(['permanent','rerunnable-limited','limited-with-permanent-alternative']);
  const RELEASE_STATUSES=new Set(['reserved','private-candidate','active']);
  const EXPECTED_SIMULATION_PINS={
    contractSha256:'44a9a1366de9eb217f72f79289b89f7d9a0738cb50bc6c88901813e1eb32ab9e',
    candidateSha256:'00f37707f72e08ed71d9d649cda2155858740059c85f7799ed6705ee8544fb5a',
    generatorSha256:'e2f06fe9979fc32eb9649cd28efa7a901ecf4e36cdb329671b1d0a4c38996436',
    machineReportSha256:'45603077138a24be83f0a17968807305772f00cc94b92f6b1a5ad87459175324',
    humanReportSha256:'4883d1cb741958f03d7bf289d81e1575320c82457f3b17cddaa9458eae3d9dfb',
    modelManifestSha256:'fdf683f84b4339f4ba74c9c8c2c51a6c86cde96411056db277bc46f26882563d',
    independentQaContractSha256:'ff4e746a19df67a08063ed2f4025cbc65b6b5b18d588278cc65e82e0a1fec365',
    independentQaResultSha256:'e246427f055f02772017d1817af3410b764ba75bfa5b4384f523ecdb00153076',
    independentFixtureSha256:'6dc03c1b7c0119a324535f59373551ab2fc44f94a0aa5fe67657379965ed2b3f',
    independentVerifierSha256:'6ce5d7dfd69ba66dbaa89369a5d2453c5b965cf86ca002ca45ace172a681163c',
    independentManifestSha256:'d37bbc747f6a4a7bb37a03b20ba3d724cb8b283891a07ba054089d745a840f5c'
  };
  const EXPECTED_TABLE_HASHES={fellow:'36c9af018a8e098d4aa2ee068e427eee8aebf8824682842a6e559910e0a591e0',companion:'f88fd74c2fe50abf7c4718948996c46bf4aaa31d35d098e43ce8fd46676b18f6',releasedRequirements:'8780262c2fb105fd685704826cd1088571250f60af8a2e5b4eb666f9983d34f7'};
  const EXPECTED_REQUIREMENT_ROWS={
    'requirements.broken-roads.book-1.v1':[22000,28500,36000,45000,56000,69000,84000,101000,121000,144000],
    'requirements.companion-campaign.phase-23.v1':[2000,2360,2785,3286,3878,4576,5399,6371,7518,8871],
    'requirements.companion-tower.phase-23.v1':[2000,2120,2247,2382,2525,2676,2837,3007,3188,3379,3582,3797,4024,4266,4522,4793,5081,5386,5709,6051,6414,6799,7207,7639,8098,8584,9099,9645,10223,10837,11487,12176,12907,13681,14502,15372,16295,17272,18309,19407,20571,21806,23114,24501,25971,27529,29181,30932,32788,34755],
    'requirements.fellow-expedition.phase-23.v1':[5500,5940,6415,6928,7483,8081,8728,9426,10180,10995,11874,12824,13850,14958,16155,17447,18843,20350,21978,23736,25635,27686,29901,32293,34876,37667,40680,43934,47449,51245,55345,59772,64554,69718,75296,81319,87825,94851,102439,110634,119485,129044,139367,150517,162558,175562,189607,204776,221158,238851]
  };

  const isObject=value=>Boolean(value)&&typeof value==='object'&&!Array.isArray(value);
  const clone=value=>JSON.parse(JSON.stringify(value));
  const exactKeys=(value,keys)=>isObject(value)&&Object.keys(value).length===keys.length&&keys.every(key=>Object.hasOwn(value,key));
  const validId=value=>typeof value==='string'&&ID_PATTERN.test(value)&&!value.split(/[.-]/).some(part=>['__proto__','prototype','constructor','toJSON'].includes(part));
  const safe=value=>Number.isSafeInteger(value)&&value>=0;
  const safeAdd=(left,right,label='value')=>{if(!safe(left)||!safe(right)||left>Number.MAX_SAFE_INTEGER-right)throw new RangeError(`${label} exceeds safe integer range`);return left+right};
  const freeze=value=>{if(value&&typeof value==='object'&&!Object.isFrozen(value)){for(const child of Object.values(value))freeze(child);Object.freeze(value)}return value};

  function canonicalStringify(value,seen=new Set()){
    if(value===null)return'null';
    if(typeof value==='string'||typeof value==='boolean')return JSON.stringify(value);
    if(typeof value==='number'){if(!Number.isFinite(value))throw new TypeError('Canonical values must be finite');return Object.is(value,-0)?'0':JSON.stringify(value)}
    if(Array.isArray(value)){if(seen.has(value))throw new TypeError('Canonical values must be acyclic plain data');seen.add(value);const result='['+value.map(item=>canonicalStringify(item,seen)).join(',')+']';seen.delete(value);return result}
    if(!isObject(value)||seen.has(value))throw new TypeError('Canonical values must be acyclic plain data');
    seen.add(value);const result='{'+Object.keys(value).sort().map(key=>JSON.stringify(key)+':'+canonicalStringify(value[key],seen)).join(',')+'}';seen.delete(value);return result;
  }
  function utf8(value){const bytes=[];for(let index=0;index<value.length;index++){let code=value.charCodeAt(index);if(code>=0xd800&&code<=0xdbff&&index+1<value.length){const next=value.charCodeAt(index+1);if(next>=0xdc00&&next<=0xdfff){code=0x10000+((code-0xd800)<<10)+(next-0xdc00);index++}}if(code<0x80)bytes.push(code);else if(code<0x800)bytes.push(0xc0|(code>>6),0x80|(code&63));else if(code<0x10000)bytes.push(0xe0|(code>>12),0x80|((code>>6)&63),0x80|(code&63));else bytes.push(0xf0|(code>>18),0x80|((code>>12)&63),0x80|((code>>6)&63),0x80|(code&63))}return bytes}
  function sha256(value){
    const bytes=utf8(String(value)),bitLength=bytes.length*8;bytes.push(0x80);while(bytes.length%64!==56)bytes.push(0);const high=Math.floor(bitLength/0x100000000),low=bitLength>>>0;for(let shift=24;shift>=0;shift-=8)bytes.push((high>>>shift)&255);for(let shift=24;shift>=0;shift-=8)bytes.push((low>>>shift)&255);
    const h=[0x6a09e667,0xbb67ae85,0x3c6ef372,0xa54ff53a,0x510e527f,0x9b05688c,0x1f83d9ab,0x5be0cd19],k=[0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2],rotr=(number,bits)=>(number>>>bits)|(number<<(32-bits));
    for(let offset=0;offset<bytes.length;offset+=64){const w=new Array(64);for(let index=0;index<16;index++){const base=offset+index*4;w[index]=((bytes[base]<<24)|(bytes[base+1]<<16)|(bytes[base+2]<<8)|bytes[base+3])>>>0}for(let index=16;index<64;index++){const a=w[index-15],b=w[index-2],s0=rotr(a,7)^rotr(a,18)^(a>>>3),s1=rotr(b,17)^rotr(b,19)^(b>>>10);w[index]=(w[index-16]+s0+w[index-7]+s1)>>>0}let[a,b,c,d,e,f,g,z]=h;for(let index=0;index<64;index++){const s1=rotr(e,6)^rotr(e,11)^rotr(e,25),choice=(e&f)^(~e&g),t1=(z+s1+choice+k[index]+w[index])>>>0,s0=rotr(a,2)^rotr(a,13)^rotr(a,22),majority=(a&b)^(a&c)^(b&c),t2=(s0+majority)>>>0;z=g;g=f;f=e;e=(d+t1)>>>0;d=c;c=b;b=a;a=(t1+t2)>>>0}h[0]=(h[0]+a)>>>0;h[1]=(h[1]+b)>>>0;h[2]=(h[2]+c)>>>0;h[3]=(h[3]+d)>>>0;h[4]=(h[4]+e)>>>0;h[5]=(h[5]+f)>>>0;h[6]=(h[6]+g)>>>0;h[7]=(h[7]+z)>>>0}
    return h.map(value=>value.toString(16).padStart(8,'0')).join('');
  }

  const manifestHash=sha256(canonicalStringify(definitions));
  const expectedDefinitionBytes=canonicalStringify(definitions);
  const authorityHash=authority=>sha256(canonicalStringify(authority));

  function validateDefinitionsUnsafe(candidate=definitions){
    const errors=[],fail=path=>{if(!errors.includes(path))errors.push(path)};
    if(!isObject(candidate)||canonicalStringify(candidate)!==expectedDefinitionBytes)fail('manifest.exact-frozen-identity');
    if(candidate?.version!==1||candidate?.configId!=='phase-24c-durable-foundation.v1'||candidate?.manifestId!=='phase-24c-durable-foundation-manifest.v1'||candidate?.status!=='inactive-foundation-only')fail('$');
    for(const[key,value]of Object.entries(EXPECTED_SIMULATION_PINS))if(candidate?.acceptedSimulation?.[key]!==value)fail(`acceptedSimulation.${key}`);
    if(canonicalStringify(candidate?.acceptedSimulation?.tableHashes)!==canonicalStringify(EXPECTED_TABLE_HASHES))fail('acceptedSimulation.tableHashes');
    const ranks=candidate?.rank?.table||[],released=['story.book1.prologue.waystone-call','story.book1.rank2.roadbound-arrivals','story.book1.rank3.crossroads-arrivals','story.book1.rank4.skybridge-arrivals','story.book1.rank5.covenant-arrivals'];
    if(candidate?.rank?.capacityThrough!==30||candidate?.rank?.releasedThrough!==5||ranks.length!==30)fail('rank.shape');
    for(let index=0;index<ranks.length;index++){const rank=index+1,row=ranks[index],expected=25*((rank*(rank+1)/2)-1),dependency=rank<=5?released[index]:`content.rank-${rank}.reserved`;if(row?.id!==`rank.${rank}.v1`||row.rank!==rank||row.totalExp!==expected||row.releaseState!==(rank<=5?'live-predecessor':'reserved')||canonicalStringify(row.contentDependencyIds)!==canonicalStringify([dependency]))fail(`rank.${rank}`)}
    const pools=(candidate?.collections?.pools||[]).map(row=>[row.id,row.kind,row.scope,row.application]),expectedPools=[['collection.pool.power','power','global','beside-might'],['collection.pool.earnings','earnings','global','beside-oath'],['collection.pool.exp','exp','eligible-fellow-and-companion-exp','raw-exp-only'],['collection.pool.facility','facility','one-facility-active-reward','beside-authored-active-bonus']];
    if(canonicalStringify(pools)!==canonicalStringify(expectedPools)||candidate?.collections?.grantDefinitions?.length!==0||candidate?.collections?.sharedLifetimeCapBps!==null||candidate?.collections?.overflowConversion!==null||candidate?.collections?.stressThroughBps!==100000)fail('collections');
    for(const[id,rows]of Object.entries(EXPECTED_REQUIREMENT_ROWS))if(canonicalStringify(candidate?.requirements?.tables?.find(row=>row.id===id)?.rows)!==canonicalStringify(rows))fail(`requirements.${id}`);
    if(candidate?.requirements?.tables?.length!==4)fail('requirements.count');
    const actorIds=new Set([...(candidate?.cast?.fellowIds||[]).map(id=>`fellow.${id}`),...(candidate?.cast?.familyIds||[]).map(id=>`family.${id}`)]);
    if(actorIds.size!==38||(candidate?.cast?.coverage||[]).length!==38||(candidate?.cast?.coverage||[]).some(row=>!actorIds.has(row.actorId)||row.verifiedCurrentContributionIds?.length!==0||!row.scheduledContributionIds?.length))fail('cast.coverage');
    if((candidate?.tutorials||[]).some(row=>!actorIds.has(row.primaryActorId)||!actorIds.has(row.fallbackActorId)||row.releaseState!=='reserved-inactive'||row.rewardNeutral!==true))fail('tutorials');
    if((candidate?.facilities?.ladders||[]).length!==candidate?.facilities?.ids?.length||(candidate?.facilities?.ladders||[]).some(row=>!candidate.facilities.ids.includes(row.facilityId)||row.releaseState!=='reserved-inactive'||row.thresholds?.length!==0))fail('facilities.ladders');
    const release=candidate?.releaseManifests?.[0];if(candidate?.releaseManifests?.length!==1||release?.id!=='release.phase-24c.foundation-reserved.v1'||release?.status!=='reserved'||release?.active!==false||release?.releasedRankThrough!==5||release?.contentDependencyIds?.length!==0||release?.collectionGrantDefinitionIds?.length!==0)fail('releaseManifests');
    if(candidate?.activation?.productionLoaded!==false||candidate?.activation?.activeReleaseIds?.length!==0||candidate?.activation?.emptyOriginPolicy!=='direct-current-schema-fresh-and-reset')fail('activation');
    return{ok:errors.length===0,errors};
  }
  function validateDefinitions(candidate=definitions){try{return validateDefinitionsUnsafe(candidate)}catch{return{ok:false,errors:['validation.exception']}}}

  function validateCollectionDefinition(definition,authority=definitions,{requireActive=false}={}){
    if(!exactKeys(definition,['id','releaseId','definitionVersion','rewardVersion','classification','permanentAlternativeId','claimSourceId','targetPool','facilityId','bps','releaseState']))return false;
    if(!validId(definition.id)||!validId(definition.releaseId)||!safe(definition.definitionVersion)||definition.definitionVersion<1||!safe(definition.rewardVersion)||definition.rewardVersion<1||!CLASSIFICATIONS.has(definition.classification)||!validId(definition.claimSourceId)||!POOL_IDS.has(definition.targetPool)||!safe(definition.bps)||definition.bps<1)return false;
    if(definition.targetPool==='facility'?!authority.facilities.ids.includes(definition.facilityId):definition.facilityId!==null)return false;
    if(definition.classification==='limited-with-permanent-alternative'?!validId(definition.permanentAlternativeId):definition.permanentAlternativeId!==null)return false;
    return!requireActive||definition.releaseState==='active';
  }

  function releaseSetIdentity(kind,value){return sha256(canonicalStringify([`phase24c.release-${kind}.v1`,value]))}
  function fixtureReportIdentity(profile){const projection={...clone(profile),fixtureReportIdentity:''};return releaseSetIdentity('fixture-report',projection)}
  function simulationPackageIdentity(simulationPackage){return releaseSetIdentity('simulation-package',simulationPackage)}
  function releasePackage(authority,releaseId){const release=authority.releaseManifests.find(row=>row.id===releaseId);if(!release)return null;const fixtureProfiles=(release.requirementFixtureProfileIds||[]).map(id=>authority.requirements.permanentOnlyProfiles.find(row=>row.id===id)).filter(Boolean).sort((a,b)=>a.id.localeCompare(b.id)),grantDefinitions=release.collectionGrantDefinitionIds.map(id=>authority.collections.grantDefinitions.find(row=>row.id===id)).filter(Boolean).sort((a,b)=>a.id.localeCompare(b.id)),rankDefinitions=authority.rank.table.slice(authority.rank.releasedThrough,release.releasedRankThrough),tableIds=new Set(fixtureProfiles.flatMap(profile=>profile.requirementTableIds)),requirementTables=[...authority.requirements.tables].filter(row=>tableIds.has(row.id)).sort((a,b)=>a.id.localeCompare(b.id)),simulationPackage=(authority.simulationPackages||[]).find(row=>row.id===release.simulationPackageId)||null;return{configId:authority.configId,release:clone(release),simulationPackage:clone(simulationPackage),grantDefinitions:clone(grantDefinitions),rankDefinitions:clone(rankDefinitions),fixtureProfiles:clone(fixtureProfiles),requirementTables:clone(requirementTables)}}
  function releaseManifestHash(authority,releaseId){const packageProjection=releasePackage(authority,releaseId);return packageProjection?releaseSetIdentity('manifest',packageProjection):null}

  function validateReleaseAuthorityUnsafe(authority=definitions){
    const errors=[],fail=path=>{if(!errors.includes(path))errors.push(path)};
    if(!isObject(authority)||!validId(authority.configId)||!validId(authority.manifestId)||!Array.isArray(authority.simulationPackages)||!Array.isArray(authority.releaseManifests)||!isObject(authority.collections)||!Array.isArray(authority.collections.grantDefinitions)||!isObject(authority.rank)||!safe(authority.rank.releasedThrough)||!safe(authority.rank.capacityThrough)||!isObject(authority.facilities)||!Array.isArray(authority.facilities.ids)||!isObject(authority.requirements)||!Array.isArray(authority.requirements.tables)||!Array.isArray(authority.requirements.permanentOnlyProfiles))return{ok:false,errors:['$']};
    if(authority.configId!==definitions.configId||authority.manifestId!==definitions.manifestId||canonicalStringify(authority.predecessorScaling)!==canonicalStringify(definitions.predecessorScaling)||canonicalStringify(authority.acceptedSimulation)!==canonicalStringify(definitions.acceptedSimulation))fail('foundation-pins');
    const policy=authority.collections;if(policy.mode!=='uncapped-additive-named-pools'||canonicalStringify(policy.pools)!==canonicalStringify(definitions.collections.pools)||policy.sharedLifetimeCapBps!==null||policy.overflowConversion!==null||policy.releaseBudgetsAreLifetimeCaps!==false||policy.futureCollectionsContinueRewards!==true||policy.mandatoryProgressionProfile!=='permanent-only'||policy.stressThroughBps!==100000||policy.limitedNeverRerunsRequiresPermanentAlternative!==true)fail('collections.policy');
    if(canonicalStringify(authority.facilities.ids)!==canonicalStringify(definitions.facilities.ids)||authority.requirements.runtimeUsesFrozenRowsOnly!==true||authority.requirements.authoringHelpersAreRuntimeAuthority!==false)fail('inherited-authority');
    const simulationPackageIds=new Set(),simulationPackageMap=new Map();for(const simulationPackage of authority.simulationPackages){const hashes=simulationPackage?.artifactHashes;if(!exactKeys(simulationPackage,['id','version','status','artifactHashes','tableHashes','requirementActivationApproved','collectionGrantActivationApproved','runtimeCurveActivationApproved','rewardThroughputApproved'])||!validId(simulationPackage.id)||simulationPackageIds.has(simulationPackage.id)||!safe(simulationPackage.version)||simulationPackage.version<1||!['provisional-baseline-only','approved-private-candidate'].includes(simulationPackage.status)||!isObject(hashes)||Object.keys(hashes).length<3||Object.values(hashes).some(value=>!HASH_PATTERN.test(value))||!isObject(simulationPackage.tableHashes)||Object.values(simulationPackage.tableHashes).some(value=>!HASH_PATTERN.test(value))||['requirementActivationApproved','collectionGrantActivationApproved','runtimeCurveActivationApproved','rewardThroughputApproved'].some(key=>typeof simulationPackage[key]!=='boolean'))fail(`simulation-package.${simulationPackage?.id||'unknown'}`);simulationPackageIds.add(simulationPackage?.id);simulationPackageMap.set(simulationPackage?.id,simulationPackage)}
    if(canonicalStringify(authority.simulationPackages[0])!==canonicalStringify(definitions.simulationPackages[0]))fail('simulation-package.baseline');
    const inheritedRequirementIds=new Set(definitions.requirements.tables.map(row=>row.id)),requirementIds=new Set();for(const table of authority.requirements.tables){const inherited=inheritedRequirementIds.has(table?.id);if(!isObject(table)||!validId(table.id)||requirementIds.has(table.id)||!safe(table.version)||table.version<1||typeof table.releaseState!=='string'||!table.releaseState||typeof table.roundingPolicy!=='string'||!table.roundingPolicy||!Array.isArray(table.rows)||table.rows.length===0||table.rows.some((value,index)=>!safe(value)||value<1||index>0&&value<table.rows[index-1])||!inherited&&(!exactKeys(table,['id','version','releaseState','roundingPolicy','contentScopeIds','simulationPackageId','rows'])||table.releaseState!=='active-content-backed'||!Array.isArray(table.contentScopeIds)||table.contentScopeIds.length===0||new Set(table.contentScopeIds).size!==table.contentScopeIds.length||table.contentScopeIds.some(id=>!validId(id))||!simulationPackageIds.has(table.simulationPackageId)))fail(`requirement.${table?.id||'unknown'}`);requirementIds.add(table?.id)}for(const inherited of definitions.requirements.tables){const found=authority.requirements.tables.find(row=>row.id===inherited.id);if(canonicalStringify(found)!==canonicalStringify(inherited))fail(`requirement.${inherited.id}.inherited`)}
    const releases=authority.releaseManifests,grants=authority.collections.grantDefinitions,releaseIds=new Set(),grantIds=new Set(),claimSourceIds=new Set(),grantMap=new Map();
    const rankTable=authority.rank.table||[];if(authority.rank.releasedThrough!==5||authority.rank.capacityThrough!==30||rankTable.length!==30)fail('rank.shape');
    for(let index=0;index<rankTable.length;index++){const row=rankTable[index],rank=index+1,expectedTotal=25*((rank*(rank+1)/2)-1);if(row?.id!==`rank.${rank}.v1`||row.rank!==rank||row.totalExp!==expectedTotal||!Array.isArray(row.contentDependencyIds)||row.contentDependencyIds.length===0||row.contentDependencyIds.some(id=>!validId(id))||rank<=5&&canonicalStringify(row)!==canonicalStringify(definitions.rank.table[index])||rank>5&&!['reserved','active-content-backed'].includes(row.releaseState)||rank>5&&row.releaseState==='reserved'&&canonicalStringify(row.contentDependencyIds)!==canonicalStringify([`content.rank-${rank}.reserved`])||rank>5&&row.releaseState==='active-content-backed'&&row.contentDependencyIds.some(id=>id.split(/[.-]/).includes('reserved')))fail(`rank.${rank}`)}
    const releaseSequences=new Set();for(const release of releases){if(!exactKeys(release,['id','version','sequence','status','active','contentDependencyIds','collectionGrantDefinitionIds','releasedRankThrough','permanentOnlyRequirementProfileId','requirementFixtureProfileIds','simulationPackageId','limitedContentRequired','activationEvidence'])||!validId(release.id)||releaseIds.has(release.id)||!safe(release.version)||release.version<1||!safe(release.sequence)||release.sequence<1||releaseSequences.has(release.sequence)||!RELEASE_STATUSES.has(release.status)||release.active!==(release.status==='active')||!Array.isArray(release.contentDependencyIds)||release.contentDependencyIds.some(id=>!validId(id))||new Set(release.contentDependencyIds).size!==release.contentDependencyIds.length||!Array.isArray(release.collectionGrantDefinitionIds)||new Set(release.collectionGrantDefinitionIds).size!==release.collectionGrantDefinitionIds.length||!Array.isArray(release.requirementFixtureProfileIds)||new Set(release.requirementFixtureProfileIds).size!==release.requirementFixtureProfileIds.length||release.requirementFixtureProfileIds.some(id=>!validId(id))||!safe(release.releasedRankThrough)||release.releasedRankThrough<authority.rank.releasedThrough||release.releasedRankThrough>authority.rank.capacityThrough||!(release.permanentOnlyRequirementProfileId===null||validId(release.permanentOnlyRequirementProfileId))||!(release.simulationPackageId===null||validId(release.simulationPackageId))||typeof release.limitedContentRequired!=='boolean')fail(`release.${release?.id||'unknown'}`);releaseIds.add(release?.id);releaseSequences.add(release?.sequence)}if(releases.some((release,index)=>index>0&&release.sequence<=releases[index-1].sequence))fail('release.sequence-order');
    for(const row of rankTable.slice(authority.rank.releasedThrough)){const covering=releases.filter(release=>release.active&&release.releasedRankThrough>=row.rank&&row.contentDependencyIds.every(id=>release.contentDependencyIds.includes(id)));if(row.releaseState==='active-content-backed'&&covering.length===0||row.releaseState==='reserved'&&covering.length>0)fail(`rank.${row.rank}.release-coverage`)}
    const profileIds=new Set(),referencedProfileIds=releases.flatMap(release=>release.requirementFixtureProfileIds);for(const profile of authority.requirements.permanentOnlyProfiles){if(!validId(profile?.id)||profileIds.has(profile.id))fail(`profile.${profile?.id||'unknown'}`);profileIds.add(profile?.id)}if(new Set(referencedProfileIds).size!==referencedProfileIds.length||referencedProfileIds.some(id=>!profileIds.has(id))||profileIds.size!==referencedProfileIds.length)fail('profiles.references');
    for(const grant of grants){if(!validateCollectionDefinition(grant,authority)||grantIds.has(grant.id)||claimSourceIds.has(grant.claimSourceId)||!releaseIds.has(grant.releaseId))fail(`grant.${grant?.id||'unknown'}`);grantIds.add(grant?.id);claimSourceIds.add(grant?.claimSourceId);grantMap.set(grant?.id,grant)}
    for(const release of releases)for(const id of release.collectionGrantDefinitionIds||[]){const grant=grantMap.get(id);if(!grant||grant.releaseId!==release.id)fail(`release.${release.id}.grant.${id}`)}
    const alternativeTargets=new Set();for(const grant of grants){const release=releases.find(row=>row.id===grant.releaseId);if(!release?.collectionGrantDefinitionIds.includes(grant.id))fail(`grant.${grant.id}.release`);if(grant.releaseState==='active'&&!release?.active)fail(`grant.${grant.id}.inactive-release`);if(grant.classification==='limited-with-permanent-alternative'){const alternative=grantMap.get(grant.permanentAlternativeId),alternativeRelease=releases.find(row=>row.id===alternative?.releaseId);if(alternativeTargets.has(grant.permanentAlternativeId)||!alternative||alternative.classification!=='permanent'||alternative.releaseState!=='active'||!alternativeRelease?.active||alternativeRelease.sequence>release.sequence||alternative.targetPool!==grant.targetPool||alternative.facilityId!==grant.facilityId||alternative.bps!==grant.bps)fail(`grant.${grant.id}.alternative`);alternativeTargets.add(grant.permanentAlternativeId)}}
    for(const release of releases){
      if(!release.active){if(release.activationEvidence!==null||release.permanentOnlyRequirementProfileId!==null||release.requirementFixtureProfileIds.length!==0||release.simulationPackageId!==null)fail(`release.${release.id}.inactive-evidence`);continue}
      const evidence=release.activationEvidence,simulationPackage=simulationPackageMap.get(release.simulationPackageId),releaseGrants=release.collectionGrantDefinitionIds.map(id=>grantMap.get(id)).sort((a,b)=>a.id.localeCompare(b.id)),rankRows=authority.rank.table.slice(authority.rank.releasedThrough,release.releasedRankThrough),fixtureProfiles=release.requirementFixtureProfileIds.map(id=>authority.requirements.permanentOnlyProfiles.find(row=>row.id===id)).filter(Boolean),profileKinds=['zero-permanent','median-permanent','high-permanent','high-all-content'],obtainableGrants=grants.filter(grant=>{const owner=releases.find(row=>row.id===grant.releaseId);return owner?.active&&owner.sequence<=release.sequence&&grant.releaseState==='active'}),obtainablePermanentIds=obtainableGrants.filter(grant=>grant.classification==='permanent').map(grant=>grant.id).sort(),alternativeTargetIds=new Set(obtainableGrants.filter(grant=>grant.classification==='limited-with-permanent-alternative').map(grant=>grant.permanentAlternativeId)),obtainableAllIds=obtainableGrants.filter(grant=>!alternativeTargetIds.has(grant.id)).map(grant=>grant.id).sort(),profileTableIdSet=new Set();let obtainablePermanentTotals=emptyCollectionTotals(),obtainableLimitedTotals=emptyCollectionTotals(),obtainableHighAllTotals=emptyCollectionTotals();for(const grant of obtainableGrants){if(grant.classification==='permanent')obtainablePermanentTotals=totalsAdd(obtainablePermanentTotals,grant.targetPool,grant.facilityId,grant.bps);else obtainableLimitedTotals=totalsAdd(obtainableLimitedTotals,grant.targetPool,grant.facilityId,grant.bps);if(obtainableAllIds.includes(grant.id))obtainableHighAllTotals=totalsAdd(obtainableHighAllTotals,grant.targetPool,grant.facilityId,grant.bps)}
      if(fixtureProfiles.length!==4||new Set(fixtureProfiles.map(profile=>profile.kind)).size!==4||profileKinds.some(kind=>!fixtureProfiles.some(profile=>profile.kind===kind))||new Set(fixtureProfiles.map(profile=>canonicalStringify([...profile.requirementTableIds].sort()))).size!==1)fail(`release.${release.id}.fixture-profiles`);
      for(const profile of fixtureProfiles){const contributorIds=Array.isArray(profile?.contributingDefinitionIds)?profile.contributingDefinitionIds:[],contributors=contributorIds.map(id=>grantMap.get(id)),tableIds=Array.isArray(profile?.requirementTableIds)?profile.requirementTableIds:[];let derived=emptyCollectionTotals();for(const grant of contributors)if(grant)derived=totalsAdd(derived,grant.targetPool,grant.facilityId,grant.bps);for(const id of tableIds)profileTableIdSet.add(id);const permanentKind=profile?.kind!=='high-all-content',expectedIds=profile?.kind==='zero-permanent'?[]:profile?.kind==='high-permanent'?obtainablePermanentIds:profile?.kind==='high-all-content'?obtainableAllIds:null,scopedTables=authority.requirements.tables.filter(table=>tableIds.includes(table.id));if(!exactKeys(profile,['id','version','kind','ownershipBand','status','contributingDefinitionIds','collectionBpsByPool','limitedContentRequired','requirementTableIds','fixtureReportIdentity'])||!safe(profile.version)||profile.version<1||profile.status!=='accepted-private-candidate'||profile.ownershipBand!==profile.kind||!profileKinds.includes(profile.kind)||new Set(contributorIds).size!==contributorIds.length||contributors.some(grant=>!grant||!obtainableGrants.includes(grant)||permanentKind&&grant.classification!=='permanent')||expectedIds&&canonicalStringify([...contributorIds].sort())!==canonicalStringify(expectedIds)||profile.kind==='median-permanent'&&contributors.some(grant=>grant.classification!=='permanent')||canonicalStringify(profile.collectionBpsByPool)!==canonicalStringify(derived)||profile.limitedContentRequired!==(profile.kind==='high-all-content'&&contributors.some(grant=>grant.classification!=='permanent'))||tableIds.length===0||new Set(tableIds).size!==tableIds.length||tableIds.some(id=>!requirementIds.has(id))||scopedTables.some(table=>!inheritedRequirementIds.has(table.id)&&table.contentScopeIds.some(id=>!release.contentDependencyIds.includes(id)))||profile.fixtureReportIdentity!==fixtureReportIdentity(profile))fail(`release.${release.id}.profile.${profile?.kind||'unknown'}`)}
      const highPermanent=fixtureProfiles.find(profile=>profile.kind==='high-permanent'),tableRows=authority.requirements.tables.filter(row=>profileTableIdSet.has(row.id)).sort((a,b)=>a.id.localeCompare(b.id));
      const activatesRequirementAuthority=rankRows.length>0||tableRows.some(table=>!inheritedRequirementIds.has(table.id)),activatesCollectionRewards=releaseGrants.length>0;
      if(!simulationPackage||simulationPackage.status!=='approved-private-candidate'||activatesRequirementAuthority&&!simulationPackage.requirementActivationApproved||activatesCollectionRewards&&(!simulationPackage.collectionGrantActivationApproved||!simulationPackage.rewardThroughputApproved)||tableRows.some(table=>!inheritedRequirementIds.has(table.id)&&table.simulationPackageId!==release.simulationPackageId)||rankRows.some(row=>row.releaseState!=='active-content-backed'||row.contentDependencyIds.some(id=>!release.contentDependencyIds.includes(id)))||release.contentDependencyIds.length===0||release.limitedContentRequired!==false||release.permanentOnlyRequirementProfileId!==highPermanent?.id||!exactKeys(evidence,['predecessorScalingIdentity','simulationPackageIdentity','contentDependencySetIdentity','rankDefinitionSetIdentity','collectionGrantDefinitionSetIdentity','requirementTableSetIdentity','fixtureProfileSetIdentity','permanentOnlyProfileIdentity','obtainablePermanentTotals','obtainableLimitedTotals','obtainableHighAllTotals','releaseBudgetReportSha256','longHorizonReportSha256','safeIntegerReportSha256'])||canonicalStringify(evidence.obtainablePermanentTotals)!==canonicalStringify(obtainablePermanentTotals)||canonicalStringify(evidence.obtainableLimitedTotals)!==canonicalStringify(obtainableLimitedTotals)||canonicalStringify(evidence.obtainableHighAllTotals)!==canonicalStringify(obtainableHighAllTotals)||!HASH_PATTERN.test(evidence.releaseBudgetReportSha256||'')||!HASH_PATTERN.test(evidence.longHorizonReportSha256||'')||!HASH_PATTERN.test(evidence.safeIntegerReportSha256||'')||evidence.predecessorScalingIdentity!==releaseSetIdentity('predecessor-scaling',authority.predecessorScaling)||evidence.simulationPackageIdentity!==simulationPackageIdentity(simulationPackage)||evidence.contentDependencySetIdentity!==releaseSetIdentity('content-dependencies',[...release.contentDependencyIds].sort())||evidence.rankDefinitionSetIdentity!==releaseSetIdentity('rank-definitions',rankRows)||evidence.collectionGrantDefinitionSetIdentity!==releaseSetIdentity('collection-grants',releaseGrants)||evidence.requirementTableSetIdentity!==releaseSetIdentity('requirement-tables',tableRows)||evidence.fixtureProfileSetIdentity!==releaseSetIdentity('fixture-profiles',[...fixtureProfiles].sort((a,b)=>a.id.localeCompare(b.id)))||evidence.permanentOnlyProfileIdentity!==releaseSetIdentity('permanent-only-profile',highPermanent))fail(`release.${release.id}.activation-evidence`)
    }
    return{ok:errors.length===0,errors};
  }
  function validateReleaseAuthority(authority=definitions){try{return validateReleaseAuthorityUnsafe(authority)}catch{return{ok:false,errors:['validation.exception']}}}

  function emptyCollectionTotals(){return{powerBps:0,earningsBps:0,expBps:0,facilityBpsByFacilityId:{}}}
  function emptyClassificationCounts(){return{permanent:0,'rerunnable-limited':0,'limited-with-permanent-alternative':0}}
  function foldedDefinitionIdsIdentity(ids){return sha256(canonicalStringify(['phase24c.folded-definition-ids.v1',[...ids].sort()]))}
  function checkpointIdentity(saveId,checkpoint){const projection={...clone(checkpoint),identity:''};return sha256(canonicalStringify(['phase24c.collection-checkpoint.v2',saveId,projection]))}
  function createEmptyCheckpoint(saveId){const checkpoint={throughSequence:0,totals:emptyCollectionTotals(),contributionCount:0,provenanceByReleaseId:{},foldedDefinitionIdsIdentity:foldedDefinitionIdsIdentity([]),priorCheckpointIdentity:null,foldedTailReceiptIdentity:null,identity:''};checkpoint.identity=checkpointIdentity(saveId,checkpoint);return checkpoint}
  function detectBootstrapPolicy(state){return(state?.saveMeta?.appliedMigrations||[]).some(item=>item?.id==='schema-12-to-13')?'predecessor-through-migration':'direct-schema-13'}
  function rawMatchesState(raw,state){if(typeof raw!=='string')return false;try{return canonicalStringify(JSON.parse(raw))===canonicalStringify(state)}catch{return false}}
  function preSuccessorCheckpointIdentity(checkpoint){const projection={...clone(checkpoint),identity:''};return sha256(canonicalStringify([CHECKPOINT_KIND,projection]))}
  function capturePreSuccessorCheckpoint(predecessor,predecessorRaw,{checkpointId}){if(predecessor?.schemaVersion!==13||typeof predecessor.saveMeta?.saveId!=='string'||!predecessor.saveMeta.saveId||!rawMatchesState(predecessorRaw,predecessor)||!validId(checkpointId))throw new TypeError('Exact schema-13 bytes and a stable checkpoint ID are required');const checkpoint={kind:CHECKPOINT_KIND,id:checkpointId,saveId:predecessor.saveMeta.saveId,rawIdentity:sha256(predecessorRaw),semanticIdentity:sha256(canonicalStringify(predecessor)),exactByteLength:utf8(predecessorRaw).length,writeOnceVerified:false,identity:''};checkpoint.identity=preSuccessorCheckpointIdentity(checkpoint);return freeze(checkpoint)}
  function attestPreSuccessorCheckpoint(checkpoint,{rereadRaw}){const draft=clone(checkpoint);if(!exactKeys(draft,['kind','id','saveId','rawIdentity','semanticIdentity','exactByteLength','writeOnceVerified','identity'])||draft.kind!==CHECKPOINT_KIND||typeof draft.saveId!=='string'||!draft.saveId||draft.writeOnceVerified!==false||draft.identity!==preSuccessorCheckpointIdentity(draft)||typeof rereadRaw!=='string'||sha256(rereadRaw)!==draft.rawIdentity||utf8(rereadRaw).length!==draft.exactByteLength)throw new TypeError('The write-once predecessor checkpoint did not reread exactly');draft.writeOnceVerified=true;draft.identity='';draft.identity=preSuccessorCheckpointIdentity(draft);return freeze(draft)}
  function validateResolvedCheckpoint(checkpoint,raw,expectedIdentity){if(!exactKeys(checkpoint,['kind','id','saveId','rawIdentity','semanticIdentity','exactByteLength','writeOnceVerified','identity'])||checkpoint.kind!==CHECKPOINT_KIND||typeof checkpoint.saveId!=='string'||!checkpoint.saveId||checkpoint.writeOnceVerified!==true||checkpoint.identity!==expectedIdentity||checkpoint.identity!==preSuccessorCheckpointIdentity(checkpoint)||typeof raw!=='string'||sha256(raw)!==checkpoint.rawIdentity||utf8(raw).length!==checkpoint.exactByteLength)return null;try{const predecessor=JSON.parse(raw);return predecessor?.saveMeta?.saveId===checkpoint.saveId&&sha256(canonicalStringify(predecessor))===checkpoint.semanticIdentity?predecessor:null}catch{return null}}

  function directOriginAttestationIdentity(attestation){const projection={...clone(attestation),identity:''};return sha256(canonicalStringify([DIRECT_ORIGIN_KIND,projection]))}
  function validateDirectOriginContract(origin,lineageKind,validator,validatorId){try{return typeof validator==='function'&&validId(validatorId)&&validator(clone(origin),lineageKind)===true}catch{return false}}
  function captureDirectOriginAttestation(origin,{lineageKind,predecessorValidatorId,directOriginValidatorId}){
    const meta=origin?.saveMeta,marker=meta?.retainedCheckpointLineage,expectedSource=lineageKind==='direct-schema-14'?'fresh':lineageKind==='safe-reset-schema-14'?'safe-reset':null;
    const semanticResetPair=isObject(marker)&&typeof marker.preResetSaveId==='string'&&marker.preResetSaveId.length>0&&marker.preResetSaveId!==meta?.saveId&&safe(marker.preResetRevision)&&marker.preResetRevision>=1;
    const forensicResetPair=isObject(marker)&&marker.version===9&&marker.kind==='safe-reset-retained-checkpoints'&&marker.saveId===meta?.saveId&&marker.resetAt===meta?.createdAt&&marker.preResetSaveId===null&&marker.preResetRevision===null&&RAW_IDENTITY_PATTERN.test(marker.preResetActiveRawIdentity||'')&&HASH_PATTERN.test(marker.phase24cPreviousInstallationIdentity||'');
    const resetPair=lineageKind!=='safe-reset-schema-14'||isObject(marker)&&(semanticResetPair||forensicResetPair)&&(!Object.hasOwn(marker,'saveId')||marker.saveId===meta?.saveId);
    if(!expectedSource||origin?.schemaVersion!==13||Object.hasOwn(origin,ROOT_KEY)||!isObject(meta)||typeof meta.saveId!=='string'||!meta.saveId||meta.revision!==1||!safe(meta.createdAt)||meta.updatedAt!==meta.createdAt||meta.source!==expectedSource||!Array.isArray(meta.appliedMigrations)||meta.appliedMigrations.length!==0||!validId(predecessorValidatorId)||!validId(directOriginValidatorId)||lineageKind==='direct-schema-14'&&marker!==undefined||lineageKind==='safe-reset-schema-14'&&!isObject(marker)||!resetPair)throw new TypeError('A canonical direct schema-14 origin is required');
    const attestation={kind:DIRECT_ORIGIN_KIND,lineageKind,saveId:meta.saveId,createdAt:meta.createdAt,source:meta.source,predecessorValidatorId,directOriginValidatorId,semanticIdentity:sha256(canonicalStringify(origin)),resetLineageIdentity:marker===undefined?null:sha256(canonicalStringify(marker)),identity:''};attestation.identity=directOriginAttestationIdentity(attestation);return freeze(attestation);
  }
  function resolvedDirectOrigin(attestation,resolver){if(!exactKeys(attestation,['kind','lineageKind','saveId','createdAt','source','predecessorValidatorId','directOriginValidatorId','semanticIdentity','resetLineageIdentity','identity'])||attestation.kind!==DIRECT_ORIGIN_KIND||!LINEAGE_KINDS.has(attestation.lineageKind)||attestation.lineageKind==='schema-13-to-14-migration'||typeof attestation.saveId!=='string'||!attestation.saveId||!safe(attestation.createdAt)||!validId(attestation.predecessorValidatorId)||!validId(attestation.directOriginValidatorId)||!HASH_PATTERN.test(attestation.semanticIdentity)||!(attestation.resetLineageIdentity===null||HASH_PATTERN.test(attestation.resetLineageIdentity))||attestation.identity!==directOriginAttestationIdentity(attestation)||typeof resolver!=='function')return null;let resolved;try{resolved=resolver(attestation.identity)}catch{return null}const origin=isObject(resolved?.state)?resolved.state:resolved;if(!isObject(origin)||origin.schemaVersion!==13||origin.saveMeta?.saveId!==attestation.saveId||origin.saveMeta?.createdAt!==attestation.createdAt||origin.saveMeta?.updatedAt!==attestation.createdAt||origin.saveMeta?.revision!==1||origin.saveMeta?.source!==attestation.source||sha256(canonicalStringify(origin))!==attestation.semanticIdentity)return null;const marker=origin.saveMeta?.retainedCheckpointLineage,resetIdentity=marker===undefined?null:sha256(canonicalStringify(marker));return resetIdentity===attestation.resetLineageIdentity?origin:null}
  function createInactiveRoot(state,at,lineage){return{configId:definitions.configId,manifestId:definitions.manifestId,manifestHash,activatedAt:at,activeReleaseIds:[],ladders:{rank:{capacityThrough:30,releasedThrough:5,pendingTransitionIds:[],acknowledgedTransitionIds:[]},family:{alignmentStatus:'reserved-inactive',alignedDefinitionIds:[],narrativeDefinitionIds:[]},legacy:{activeTrackIds:[],activeTierIds:[]},events:{activeTrackIds:[]},facilities:{activeLadderIds:[]}},collections:{checkpoint:createEmptyCheckpoint(state.saveMeta.saveId),recentGrantReceipts:[],claimedDefinitionIds:[]},migrations:{lineageKind:lineage.lineageKind,activationReceiptId:lineage.activationReceiptId,familyAlignmentReceiptId:null,predecessorSemanticIdentity:lineage.predecessorSemanticIdentity,predecessorRawIdentity:lineage.predecessorRawIdentity,predecessorCheckpointIdentity:lineage.predecessorCheckpointIdentity,bootstrapPolicy:lineage.bootstrapPolicy,resetLineagePolicy:lineage.resetLineagePolicy,directOriginAttestation:lineage.directOriginAttestation}}}
  function migrationReceiptIdentity(saveId,receipt){const projection={...clone(receipt),identity:''};return sha256(canonicalStringify(['phase24c.migration-receipt.v2',saveId,projection]))}
  function migrationTargetProjectionIdentity(state){const projection=clone(state),receipt=projection.saveMeta.appliedMigrations.find(item=>item?.id===MIGRATION_ID);if(receipt){receipt.activationTargetIdentity='';receipt.identity=''}return sha256(canonicalStringify(projection))}
  function validatePredecessorContract(predecessor,validator,validatorId){try{return typeof validator==='function'&&validId(validatorId)&&validator(clone(predecessor))===true}catch{return false}}
  function currentSchema13Projection(state){const projection=clone(state);projection.schemaVersion=13;delete projection[ROOT_KEY];if(Array.isArray(projection.saveMeta?.appliedMigrations))projection.saveMeta.appliedMigrations=projection.saveMeta.appliedMigrations.filter(item=>item?.id!==MIGRATION_ID);return projection}
  function buildActivationTarget(predecessor,{now,source,predecessorValidatorId,checkpoint}){const next=clone(predecessor),priorMeta=clone(predecessor.saveMeta),bootstrapPolicy=detectBootstrapPolicy(predecessor),resetLineagePolicy=isObject(predecessor.saveMeta.retainedCheckpointLineage)?'predecessor-reset-lineage-bound-v1':'not-applicable',lineage={lineageKind:'schema-13-to-14-migration',activationReceiptId:MIGRATION_ID,predecessorSemanticIdentity:checkpoint.semanticIdentity,predecessorRawIdentity:checkpoint.rawIdentity,predecessorCheckpointIdentity:checkpoint.identity,bootstrapPolicy,resetLineagePolicy,directOriginAttestation:null};next.schemaVersion=SUCCESSOR_SCHEMA;next.saveMeta.revision=safeAdd(priorMeta.revision,1,'Migration revision');next.saveMeta.updatedAt=now;next.saveMeta.source=source;next[ROOT_KEY]=createInactiveRoot(predecessor,now,lineage);const receipt={id:MIGRATION_ID,from:13,to:14,receiptVersion:2,appliedAt:now,source,configId:definitions.configId,manifestId:definitions.manifestId,manifestHash,predecessorSemanticIdentity:checkpoint.semanticIdentity,predecessorRawIdentity:checkpoint.rawIdentity,predecessorCheckpointIdentity:checkpoint.identity,predecessorRevision:priorMeta.revision,predecessorUpdatedAt:priorMeta.updatedAt,predecessorSource:priorMeta.source,predecessorValidatorId,bootstrapPolicy,activeReleaseIds:[],rewardApplications:0,activationTargetIdentity:'',identity:''};next.saveMeta.appliedMigrations.push(receipt);receipt.activationTargetIdentity=migrationTargetProjectionIdentity(next);receipt.identity=migrationReceiptIdentity(next.saveMeta.saveId,receipt);return next}
  function buildDirectActivationTarget(origin,{lineageKind,attestation}){const next=clone(origin),lineage={lineageKind,activationReceiptId:null,predecessorSemanticIdentity:null,predecessorRawIdentity:null,predecessorCheckpointIdentity:null,bootstrapPolicy:lineageKind,resetLineagePolicy:lineageKind==='safe-reset-schema-14'?'protected-previous-installation-v1':'not-applicable',directOriginAttestation:clone(attestation)};next.schemaVersion=SUCCESSOR_SCHEMA;next[ROOT_KEY]=createInactiveRoot(origin,origin.saveMeta.createdAt,lineage);return next}
  function createDirectSchema14(origin,{lineageKind='direct-schema-14',validatePredecessor=null,predecessorValidatorId=null,validateDirectOrigin=null,directOriginValidatorId=null,resolveDirectOrigin=null,authority=definitions}={}){if(authority!==definitions)throw new TypeError('Initial direct activation must use the frozen inactive foundation authority');if(!validatePredecessorContract(origin,validatePredecessor,predecessorValidatorId)||!validateDirectOriginContract(origin,lineageKind,validateDirectOrigin,directOriginValidatorId))throw new TypeError('Named schema-13 and direct-origin validators must approve activation');const attestation=captureDirectOriginAttestation(origin,{lineageKind,predecessorValidatorId,directOriginValidatorId}),next=buildDirectActivationTarget(origin,{lineageKind,attestation}),localResolver=identity=>identity===attestation.identity?clone(origin):typeof resolveDirectOrigin==='function'?resolveDirectOrigin(identity):null,context={validatePredecessor,predecessorValidatorId,validateDirectOrigin,directOriginValidatorId,resolveDirectOrigin:localResolver,authority},checked=validateSuccessorState(next,context);if(!checked.ok)throw new TypeError(`Phase 24C direct activation produced invalid state: ${checked.errors.join(', ')}`);return{changed:true,state:next,receipt:null,attestation:clone(attestation)}}
  function migrateSchema13To14(predecessor,{now,source='schema-13-to-14-inactive-foundation',validatePredecessor=null,predecessorValidatorId=null,predecessorCheckpoint=null,resolvePredecessorCheckpoint=null,validateDirectOrigin=null,directOriginValidatorId=null,resolveDirectOrigin=null,authority=definitions}={}){const context={validatePredecessor,predecessorValidatorId,resolvePredecessorCheckpoint,validateDirectOrigin,directOriginValidatorId,resolveDirectOrigin,authority};if(predecessor?.schemaVersion===SUCCESSOR_SCHEMA){const checked=validateSuccessorState(predecessor,context);if(!checked.ok)throw new TypeError(`Invalid Phase 24C successor: ${checked.errors.join(', ')}`);const receipt=predecessor.saveMeta.appliedMigrations.find(item=>item.id===MIGRATION_ID);return{changed:false,state:clone(predecessor),receipt:receipt?clone(receipt):null}}if(authority!==definitions)throw new TypeError('Initial schema-13 activation must use the frozen inactive foundation authority');if(!isObject(predecessor)||predecessor.schemaVersion!==13||!isObject(predecessor.saveMeta)||typeof predecessor.saveMeta.saveId!=='string'||!predecessor.saveMeta.saveId||!safe(predecessor.saveMeta.revision)||predecessor.saveMeta.revision<1||!safe(predecessor.saveMeta.createdAt)||!safe(predecessor.saveMeta.updatedAt)||predecessor.saveMeta.updatedAt<predecessor.saveMeta.createdAt||typeof predecessor.saveMeta.source!=='string'||!predecessor.saveMeta.source||!Array.isArray(predecessor.saveMeta.appliedMigrations)||Object.hasOwn(predecessor,ROOT_KEY))throw new TypeError('A valid schema-13 predecessor is required');if(!validatePredecessorContract(predecessor,validatePredecessor,predecessorValidatorId))throw new TypeError('A named schema-13 predecessor validator must approve migration');if(!safe(now)||now<predecessor.saveMeta.updatedAt||typeof source!=='string'||!source)throw new TypeError('Migration time or source is invalid');const resolved=typeof resolvePredecessorCheckpoint==='function'?resolvePredecessorCheckpoint(predecessorCheckpoint?.identity):null,raw=typeof resolved==='string'?resolved:resolved?.raw,checkpoint=resolved?.checkpoint||predecessorCheckpoint,exactPredecessor=validateResolvedCheckpoint(checkpoint,raw,predecessorCheckpoint?.identity);if(!exactPredecessor||canonicalStringify(exactPredecessor)!==canonicalStringify(predecessor))throw new TypeError('A verified write-once exact predecessor checkpoint is required');const next=buildActivationTarget(predecessor,{now,source,predecessorValidatorId,checkpoint}),checked=validateSuccessorState(next,context);if(!checked.ok)throw new TypeError(`Phase 24C migration produced invalid state: ${checked.errors.join(', ')}`);return{changed:true,state:next,receipt:clone(next.saveMeta.appliedMigrations.at(-1))}}

  function totalsAdd(totals,targetPool,facilityId,bps){const result=clone(totals);if(targetPool==='facility')result.facilityBpsByFacilityId[facilityId]=safeAdd(result.facilityBpsByFacilityId[facilityId]||0,bps,'Facility Collection total');else result[`${targetPool}Bps`]=safeAdd(result[`${targetPool}Bps`],bps,`Collection ${targetPool} total`);return result}
  function poolTotal(totals,targetPool,facilityId){return targetPool==='facility'?(totals.facilityBpsByFacilityId[facilityId]||0):totals[`${targetPool}Bps`]}
  function collectionTotals(state){const collections=state?.[ROOT_KEY]?.collections;if(!collections)return null;let totals=clone(collections.checkpoint.totals);for(const receipt of collections.recentGrantReceipts)totals=totalsAdd(totals,receipt.targetPool,receipt.facilityId,receipt.bps);return totals}
  function collectionPayloadIdentity(saveId,payload){const projection={...clone(payload),identity:''};return sha256(canonicalStringify(['phase24c.collection-grant-payload.v2',saveId,projection]))}
  function collectionPreviewIdentity(saveId,preview){const projection={...clone(preview),identity:''};return sha256(canonicalStringify(['phase24c.collection-grant-preview.v1',saveId,projection]))}
  function collectionReceiptIdentity(saveId,receipt){const projection={...clone(receipt),identity:''};return sha256(canonicalStringify(['phase24c.collection-grant-receipt.v2',saveId,projection]))}
  function findGrant(authority,id){return authority.collections.grantDefinitions.find(row=>row.id===id)}
  function definitionBlockedByAlternative(claimedIds,definition,authority){if(definition.permanentAlternativeId&&claimedIds.includes(definition.permanentAlternativeId))return true;return authority.collections.grantDefinitions.some(row=>row.permanentAlternativeId===definition.id&&claimedIds.includes(row.id))}
  function validationOptions(options={}){return{authority:options.authority||definitions,validatePredecessor:options.validatePredecessor,predecessorValidatorId:options.predecessorValidatorId,resolvePredecessorCheckpoint:options.resolvePredecessorCheckpoint,validateDirectOrigin:options.validateDirectOrigin,directOriginValidatorId:options.directOriginValidatorId,resolveDirectOrigin:options.resolveDirectOrigin}}
  function validatePayloadAgainstAuthority(payload,state,authority){if(!isObject(payload)||payload.identity!==collectionPayloadIdentity(state.saveMeta.saveId,payload)||payload.configId!==authority.configId||payload.releaseManifestId!==payload.releaseId||payload.releaseManifestHash!==releaseManifestHash(authority,payload.releaseId))return null;const definition=findGrant(authority,payload.definitionId);if(!definition||!validateCollectionDefinition(definition,authority,{requireActive:true}))return null;for(const key of ['releaseId','definitionVersion','rewardVersion','classification','permanentAlternativeId','claimSourceId','targetPool','facilityId','bps'])if(payload[key]!==definition[key])return null;return definition}
  function captureCollectionGrantPayload(state,definitionOrId,{capturedAt,...options}){
    const authority=options.authority||definitions,checked=validateSuccessorState(state,validationOptions(options)),definition=typeof definitionOrId==='string'?findGrant(authority,definitionOrId):definitionOrId,canonical=findGrant(authority,definition?.id);
    if(!checked.ok)throw new TypeError('A valid Phase 24C successor state is required');
    if(!validateReleaseAuthority(authority).ok||!canonical||canonicalStringify(canonical)!==canonicalStringify(definition)||!validateCollectionDefinition(canonical,authority,{requireActive:true}))throw new TypeError('An immutable active Collection definition is required');
    const release=authority.releaseManifests.find(row=>row.id===canonical.releaseId),claimed=state[ROOT_KEY].collections.claimedDefinitionIds;
    if(!release?.active||!release.collectionGrantDefinitionIds.includes(canonical.id)||!state[ROOT_KEY].activeReleaseIds.includes(release.id))throw new TypeError('The Collection release is not active');
    if(claimed.includes(canonical.id)||definitionBlockedByAlternative(claimed,canonical,authority))throw new TypeError('Collection definition or its permanent alternative was already claimed');
    if(!safe(capturedAt)||capturedAt<state[ROOT_KEY].activatedAt)throw new TypeError('Collection capture time is invalid');
    const payload={configId:authority.configId,releaseManifestId:release.id,releaseManifestHash:releaseManifestHash(authority,release.id),definitionId:canonical.id,releaseId:canonical.releaseId,definitionVersion:canonical.definitionVersion,rewardVersion:canonical.rewardVersion,classification:canonical.classification,permanentAlternativeId:canonical.permanentAlternativeId,claimSourceId:canonical.claimSourceId,targetPool:canonical.targetPool,facilityId:canonical.facilityId,bps:canonical.bps,capturedAt,identity:''};payload.identity=collectionPayloadIdentity(state.saveMeta.saveId,payload);return freeze(payload);
  }
  function previewCollectionGrant(state,payload,{previewedAt,...options}){
    const authority=options.authority||definitions,checked=validateSuccessorState(state,validationOptions(options)),definition=checked.ok?validatePayloadAgainstAuthority(payload,state,authority):null;
    if(!checked.ok||!definition)throw new TypeError('A valid state and immutable Collection payload are required');
    const claimed=state[ROOT_KEY].collections.claimedDefinitionIds;if(claimed.includes(definition.id)||definitionBlockedByAlternative(claimed,definition,authority))throw new TypeError('Collection definition or its permanent alternative was already claimed');if(!safe(previewedAt)||previewedAt<payload.capturedAt)throw new TypeError('Collection preview time is invalid');
    const totals=collectionTotals(state),oldTotalBps=poolTotal(totals,payload.targetPool,payload.facilityId),newTotalBps=safeAdd(oldTotalBps,payload.bps,'Collection preview total'),preview={payloadIdentity:payload.identity,configId:payload.configId,releaseManifestId:payload.releaseManifestId,releaseManifestHash:payload.releaseManifestHash,targetPool:payload.targetPool,facilityId:payload.facilityId,oldTotalBps,gainBps:payload.bps,newTotalBps,previewedAt,collectionAnchorIdentity:state[ROOT_KEY].collections.recentGrantReceipts.at(-1)?.identity||(state[ROOT_KEY].collections.checkpoint.throughSequence?state[ROOT_KEY].collections.checkpoint.foldedTailReceiptIdentity:state[ROOT_KEY].collections.checkpoint.identity),identity:''};preview.identity=collectionPreviewIdentity(state.saveMeta.saveId,preview);return freeze(preview);
  }
  function addProvenance(provenance,receipt){const result=clone(provenance),row=result[receipt.releaseId]||{configId:receipt.configId,releaseManifestHash:receipt.releaseManifestHash,contributionCount:0,powerBps:0,earningsBps:0,expBps:0,facilityBpsByFacilityId:{},classificationCounts:emptyClassificationCounts()};if(row.configId!==receipt.configId||row.releaseManifestHash!==receipt.releaseManifestHash)throw new TypeError('Folded release provenance changed');row.contributionCount=safeAdd(row.contributionCount,1,'Release contribution count');if(receipt.targetPool==='facility')row.facilityBpsByFacilityId[receipt.facilityId]=safeAdd(row.facilityBpsByFacilityId[receipt.facilityId]||0,receipt.bps,'Release facility total');else row[`${receipt.targetPool}Bps`]=safeAdd(row[`${receipt.targetPool}Bps`],receipt.bps,'Release pool total');row.classificationCounts[receipt.classification]=safeAdd(row.classificationCounts[receipt.classification],1,'Release classification count');result[receipt.releaseId]=row;return result}
  function foldCollectionReceiptsInState(state){const collections=state[ROOT_KEY].collections;if(collections.recentGrantReceipts.length<=RECENT_GRANT_LIMIT)return false;const folded=collections.recentGrantReceipts.splice(0,GRANT_FOLD_BATCH),prior=collections.checkpoint;let totals=clone(prior.totals),contributionCount=prior.contributionCount,provenance=clone(prior.provenanceByReleaseId);for(const receipt of folded){totals=totalsAdd(totals,receipt.targetPool,receipt.facilityId,receipt.bps);contributionCount=safeAdd(contributionCount,1,'Collection contribution count');provenance=addProvenance(provenance,receipt)}const recentIds=new Set(collections.recentGrantReceipts.map(receipt=>receipt.definitionId)),foldedIds=collections.claimedDefinitionIds.filter(id=>!recentIds.has(id)),checkpoint={throughSequence:folded.at(-1).sequence,totals,contributionCount,provenanceByReleaseId:provenance,foldedDefinitionIdsIdentity:foldedDefinitionIdsIdentity(foldedIds),priorCheckpointIdentity:prior.identity,foldedTailReceiptIdentity:folded.at(-1).identity,identity:''};checkpoint.identity=checkpointIdentity(state.saveMeta.saveId,checkpoint);collections.checkpoint=checkpoint;return true}
  function payloadFromReceipt(receipt){return{configId:receipt.configId,releaseManifestId:receipt.releaseManifestId,releaseManifestHash:receipt.releaseManifestHash,definitionId:receipt.definitionId,releaseId:receipt.releaseId,definitionVersion:receipt.definitionVersion,rewardVersion:receipt.rewardVersion,classification:receipt.classification,permanentAlternativeId:receipt.permanentAlternativeId,claimSourceId:receipt.claimSourceId,targetPool:receipt.targetPool,facilityId:receipt.facilityId,bps:receipt.bps,capturedAt:receipt.capturedAt,identity:receipt.payloadIdentity}}
  function finalizeCollectionGrant(state,payload,preview,{claimedAt,...options}={}){
    let authority,context,draft,checked;const refuse=reason=>{let returnedState=state;try{returnedState=clone(state)}catch{}return{ok:false,reason,state:returnedState,rawUnchanged:true,rewardApplications:0}};
    try{
    authority=options.authority||definitions;context=validationOptions(options);canonicalStringify(state);draft=clone(state);checked=validateSuccessorState(draft,context);
    if(!checked.ok)return refuse('invalid-state');const definition=validatePayloadAgainstAuthority(payload,draft,authority);if(!definition)return refuse('payload-or-definition-mismatch');
    if(!isObject(preview)||preview.identity!==collectionPreviewIdentity(draft.saveMeta.saveId,preview)||preview.payloadIdentity!==payload.identity||preview.configId!==payload.configId||preview.releaseManifestId!==payload.releaseManifestId||preview.releaseManifestHash!==payload.releaseManifestHash||preview.targetPool!==payload.targetPool||preview.facilityId!==payload.facilityId||preview.gainBps!==payload.bps)return refuse('preview-identity-mismatch');
    const release=authority.releaseManifests.find(row=>row.id===payload.releaseId),claimed=draft[ROOT_KEY].collections.claimedDefinitionIds;if(!release?.active||!release.collectionGrantDefinitionIds.includes(payload.definitionId)||!draft[ROOT_KEY].activeReleaseIds.includes(payload.releaseId))return refuse('release-inactive');if(claimed.includes(payload.definitionId)||definitionBlockedByAlternative(claimed,definition,authority))return refuse('already-claimed-or-alternative');if(!safe(claimedAt)||claimedAt<preview.previewedAt)return refuse('claim-time-invalid');
    const collections=draft[ROOT_KEY].collections,anchor=collections.recentGrantReceipts.at(-1)?.identity||(collections.checkpoint.throughSequence?collections.checkpoint.foldedTailReceiptIdentity:collections.checkpoint.identity),totals=collectionTotals(draft),liveOld=poolTotal(totals,payload.targetPool,payload.facilityId),liveNew=safeAdd(liveOld,payload.bps,'Collection claim total');if(anchor!==preview.collectionAnchorIdentity||liveOld!==preview.oldTotalBps||liveNew!==preview.newTotalBps)return refuse('stale-preview');
    const sequence=safeAdd(collections.checkpoint.throughSequence,collections.recentGrantReceipts.length+1,'Collection receipt sequence'),receipt={id:`collection.receipt.${sequence}.v2`,sequence,configId:payload.configId,releaseManifestId:payload.releaseManifestId,releaseManifestHash:payload.releaseManifestHash,definitionId:payload.definitionId,releaseId:payload.releaseId,claimSourceId:payload.claimSourceId,targetPool:payload.targetPool,facilityId:payload.facilityId,bps:payload.bps,oldTotalBps:liveOld,newTotalBps:liveNew,classification:payload.classification,permanentAlternativeId:payload.permanentAlternativeId,definitionVersion:payload.definitionVersion,rewardVersion:payload.rewardVersion,capturedAt:payload.capturedAt,previewedAt:preview.previewedAt,claimedAt,payloadIdentity:payload.identity,previewIdentity:preview.identity,priorIdentity:anchor,identity:''};receipt.identity=collectionReceiptIdentity(draft.saveMeta.saveId,receipt);collections.recentGrantReceipts.push(receipt);collections.claimedDefinitionIds.push(payload.definitionId);collections.claimedDefinitionIds.sort();foldCollectionReceiptsInState(draft);const finalCheck=validateSuccessorState(draft,context);if(!finalCheck.ok)return refuse('postcondition-failed');return{ok:true,state:draft,receipt:clone(receipt),rawUnchanged:false,rewardApplications:1};
    }catch{return refuse('validation-exception')}
  }

  function validTotals(totals,authority){return exactKeys(totals,['powerBps','earningsBps','expBps','facilityBpsByFacilityId'])&&safe(totals.powerBps)&&safe(totals.earningsBps)&&safe(totals.expBps)&&isObject(totals.facilityBpsByFacilityId)&&Object.entries(totals.facilityBpsByFacilityId).every(([id,value])=>authority.facilities.ids.includes(id)&&safe(value))}
  function validProvenance(provenance,checkpoint,authority){if(!isObject(provenance))return false;let count=0,totals=emptyCollectionTotals();for(const[releaseId,row]of Object.entries(provenance)){if(!authority.releaseManifests.some(release=>release.id===releaseId)||!exactKeys(row,['configId','releaseManifestHash','contributionCount','powerBps','earningsBps','expBps','facilityBpsByFacilityId','classificationCounts'])||row.configId!==authority.configId||row.releaseManifestHash!==releaseManifestHash(authority,releaseId)||!safe(row.contributionCount)||!validTotals({powerBps:row.powerBps,earningsBps:row.earningsBps,expBps:row.expBps,facilityBpsByFacilityId:row.facilityBpsByFacilityId},authority)||!exactKeys(row.classificationCounts,[...CLASSIFICATIONS])||Object.values(row.classificationCounts).some(value=>!safe(value))||Object.values(row.classificationCounts).reduce((a,b)=>a+b,0)!==row.contributionCount)return false;count=safeAdd(count,row.contributionCount);totals=totalsAdd(totals,'power',null,row.powerBps);totals=totalsAdd(totals,'earnings',null,row.earningsBps);totals=totalsAdd(totals,'exp',null,row.expBps);for(const[id,value]of Object.entries(row.facilityBpsByFacilityId))totals=totalsAdd(totals,'facility',id,value)}return count===checkpoint.contributionCount&&canonicalStringify(totals)===canonicalStringify(checkpoint.totals)}
  function validateCollectionState(state,errors,authority){
    const saveId=state.saveMeta.saveId,collections=state[ROOT_KEY]?.collections,checkpoint=collections?.checkpoint;if(!exactKeys(collections,['checkpoint','recentGrantReceipts','claimedDefinitionIds'])){errors.push('collections');return}
    const emptyCheckpoint=checkpoint?.throughSequence===0,foldedCheckpoint=checkpoint?.throughSequence>0;
    if(!exactKeys(checkpoint,['throughSequence','totals','contributionCount','provenanceByReleaseId','foldedDefinitionIdsIdentity','priorCheckpointIdentity','foldedTailReceiptIdentity','identity'])||!safe(checkpoint.throughSequence)||!safe(checkpoint.contributionCount)||checkpoint.contributionCount!==checkpoint.throughSequence||!validTotals(checkpoint.totals,authority)||!HASH_PATTERN.test(checkpoint.foldedDefinitionIdsIdentity||'')||(emptyCheckpoint&&(checkpoint.priorCheckpointIdentity!==null||checkpoint.foldedTailReceiptIdentity!==null))||(foldedCheckpoint&&(!HASH_PATTERN.test(checkpoint.priorCheckpointIdentity||'')||!HASH_PATTERN.test(checkpoint.foldedTailReceiptIdentity||'')))||checkpoint.identity!==checkpointIdentity(saveId,checkpoint)||!validProvenance(checkpoint.provenanceByReleaseId,checkpoint,authority)){errors.push('collections.checkpoint');return}
    if(!Array.isArray(collections.recentGrantReceipts)||collections.recentGrantReceipts.length>RECENT_GRANT_LIMIT||!Array.isArray(collections.claimedDefinitionIds)||new Set(collections.claimedDefinitionIds).size!==collections.claimedDefinitionIds.length||collections.claimedDefinitionIds.some(id=>!validId(id))){errors.push('collections.archive');return}
    let expectedSequence=checkpoint.throughSequence,priorIdentity=foldedCheckpoint?checkpoint.foldedTailReceiptIdentity:checkpoint.identity,running=clone(checkpoint.totals);const recentIds=new Set();
    for(const receipt of collections.recentGrantReceipts){expectedSequence++;const keys=['id','sequence','configId','releaseManifestId','releaseManifestHash','definitionId','releaseId','claimSourceId','targetPool','facilityId','bps','oldTotalBps','newTotalBps','classification','permanentAlternativeId','definitionVersion','rewardVersion','capturedAt','previewedAt','claimedAt','payloadIdentity','previewIdentity','priorIdentity','identity'],definition=findGrant(authority,receipt?.definitionId),payload=receipt?payloadFromReceipt(receipt):null,release=authority.releaseManifests.find(row=>row.id===receipt?.releaseId),old=receipt?poolTotal(running,receipt.targetPool,receipt.facilityId):null,preview=receipt?{payloadIdentity:receipt.payloadIdentity,configId:receipt.configId,releaseManifestId:receipt.releaseManifestId,releaseManifestHash:receipt.releaseManifestHash,targetPool:receipt.targetPool,facilityId:receipt.facilityId,oldTotalBps:receipt.oldTotalBps,gainBps:receipt.bps,newTotalBps:receipt.newTotalBps,previewedAt:receipt.previewedAt,collectionAnchorIdentity:receipt.priorIdentity,identity:receipt.previewIdentity}:null;
      if(!exactKeys(receipt,keys)||receipt.id!==`collection.receipt.${expectedSequence}.v2`||receipt.sequence!==expectedSequence||receipt.configId!==authority.configId||receipt.releaseManifestId!==receipt.releaseId||receipt.releaseManifestHash!==releaseManifestHash(authority,receipt.releaseId)||!definition||!release?.collectionGrantDefinitionIds.includes(definition.id)||!validateCollectionDefinition(definition,authority,{requireActive:true})||definition.releaseId!==receipt.releaseId||definition.claimSourceId!==receipt.claimSourceId||definition.targetPool!==receipt.targetPool||definition.facilityId!==receipt.facilityId||definition.bps!==receipt.bps||definition.classification!==receipt.classification||definition.permanentAlternativeId!==receipt.permanentAlternativeId||definition.definitionVersion!==receipt.definitionVersion||definition.rewardVersion!==receipt.rewardVersion||old!==receipt.oldTotalBps||safeAdd(old,receipt.bps,'Collection validation total')!==receipt.newTotalBps||!safe(receipt.capturedAt)||!safe(receipt.previewedAt)||!safe(receipt.claimedAt)||receipt.previewedAt<receipt.capturedAt||receipt.claimedAt<receipt.previewedAt||receipt.payloadIdentity!==collectionPayloadIdentity(saveId,payload)||receipt.previewIdentity!==collectionPreviewIdentity(saveId,preview)||receipt.priorIdentity!==priorIdentity||receipt.identity!==collectionReceiptIdentity(saveId,receipt)||!collections.claimedDefinitionIds.includes(receipt.definitionId)||recentIds.has(receipt.definitionId)){errors.push(`collections.receipt.${expectedSequence}`);return}recentIds.add(receipt.definitionId);running=totalsAdd(running,receipt.targetPool,receipt.facilityId,receipt.bps);priorIdentity=receipt.identity}
    const foldedIds=collections.claimedDefinitionIds.filter(id=>!recentIds.has(id));if(collections.claimedDefinitionIds.length!==checkpoint.contributionCount+collections.recentGrantReceipts.length||foldedIds.length!==checkpoint.contributionCount||checkpoint.foldedDefinitionIdsIdentity!==foldedDefinitionIdsIdentity(foldedIds)){errors.push('collections.claimedDefinitionIds');return}for(const id of collections.claimedDefinitionIds){const definition=findGrant(authority,id);if(!definition){errors.push('collections.claimedDefinitionIds.reference');return}if(definitionBlockedByAlternative(collections.claimedDefinitionIds.filter(other=>other!==id),definition,authority)){errors.push('collections.claimedDefinitionIds.alternative');return}}
  }

  function validateSuccessorStateUnsafe(state,options={}){
    const authority=options.authority||definitions,errors=[],fail=path=>{if(!errors.includes(path))errors.push(path)},authorityCheck=validateReleaseAuthority(authority);if(!authorityCheck.ok)return{ok:false,errors:authorityCheck.errors.map(path=>`authority.${path}`)};
    if(!isObject(state)||state.schemaVersion!==SUCCESSOR_SCHEMA||!isObject(state.saveMeta)||typeof state.saveMeta.saveId!=='string'||!state.saveMeta.saveId||!safe(state.saveMeta.revision)||state.saveMeta.revision<1||!safe(state.saveMeta.createdAt)||!safe(state.saveMeta.updatedAt)||state.saveMeta.updatedAt<state.saveMeta.createdAt||typeof state.saveMeta.source!=='string'||!state.saveMeta.source||!Array.isArray(state.saveMeta.appliedMigrations))return{ok:false,errors:['$']};
    if(!validatePredecessorContract(currentSchema13Projection(state),options.validatePredecessor,options.predecessorValidatorId))fail('current-schema-13-projection');
    const root=state[ROOT_KEY],activeReleases=authority.releaseManifests.filter(row=>row.active).map(row=>row.id).sort(),releasedThrough=Math.max(authority.rank.releasedThrough,...authority.releaseManifests.filter(row=>row.active).map(row=>row.releasedRankThrough));
    if(!exactKeys(root,['configId','manifestId','manifestHash','activatedAt','activeReleaseIds','ladders','collections','migrations'])||root.configId!==authority.configId||root.manifestId!==authority.manifestId||root.manifestHash!==authorityHash(authority)||!safe(root.activatedAt)||root.activatedAt>state.saveMeta.updatedAt||canonicalStringify(root.activeReleaseIds)!==canonicalStringify(activeReleases))return{ok:false,errors:[ROOT_KEY]};
    const ladders=root.ladders;if(!exactKeys(ladders,['rank','family','legacy','events','facilities'])||!exactKeys(ladders.rank,['capacityThrough','releasedThrough','pendingTransitionIds','acknowledgedTransitionIds'])||ladders.rank.capacityThrough!==authority.rank.capacityThrough||ladders.rank.releasedThrough!==releasedThrough||!Array.isArray(ladders.rank.pendingTransitionIds)||!Array.isArray(ladders.rank.acknowledgedTransitionIds)||[...ladders.rank.pendingTransitionIds,...ladders.rank.acknowledgedTransitionIds].some(id=>!validId(id))||new Set([...ladders.rank.pendingTransitionIds,...ladders.rank.acknowledgedTransitionIds]).size!==ladders.rank.pendingTransitionIds.length+ladders.rank.acknowledgedTransitionIds.length)fail('ladders.rank');
    if(!exactKeys(ladders.family,['alignmentStatus','alignedDefinitionIds','narrativeDefinitionIds'])||ladders.family.alignmentStatus!=='reserved-inactive'||ladders.family.alignedDefinitionIds.length!==0||ladders.family.narrativeDefinitionIds.length!==0)fail('ladders.family');if(!exactKeys(ladders.legacy,['activeTrackIds','activeTierIds'])||ladders.legacy.activeTrackIds.length!==0||ladders.legacy.activeTierIds.length!==0||!exactKeys(ladders.events,['activeTrackIds'])||ladders.events.activeTrackIds.length!==0||!exactKeys(ladders.facilities,['activeLadderIds'])||ladders.facilities.activeLadderIds.length!==0)fail('ladders.inactive');
    validateCollectionState(state,errors,authority);
    const receipts=state.saveMeta.appliedMigrations.filter(item=>item?.id===MIGRATION_ID),receipt=receipts[0],migrations=root.migrations,migrationKeys=['lineageKind','activationReceiptId','familyAlignmentReceiptId','predecessorSemanticIdentity','predecessorRawIdentity','predecessorCheckpointIdentity','bootstrapPolicy','resetLineagePolicy','directOriginAttestation'],receiptKeys=['id','from','to','receiptVersion','appliedAt','source','configId','manifestId','manifestHash','predecessorSemanticIdentity','predecessorRawIdentity','predecessorCheckpointIdentity','predecessorRevision','predecessorUpdatedAt','predecessorSource','predecessorValidatorId','bootstrapPolicy','activeReleaseIds','rewardApplications','activationTargetIdentity','identity'];
    if(!exactKeys(migrations,migrationKeys)||!LINEAGE_KINDS.has(migrations?.lineageKind)||migrations?.familyAlignmentReceiptId!==null)fail('migrations.shape');
    if(migrations?.lineageKind==='schema-13-to-14-migration'){
      if(state.saveMeta.revision<2||receipts.length!==1||migrations.activationReceiptId!==MIGRATION_ID||migrations.predecessorSemanticIdentity!==receipt?.predecessorSemanticIdentity||migrations.predecessorRawIdentity!==receipt?.predecessorRawIdentity||migrations.predecessorCheckpointIdentity!==receipt?.predecessorCheckpointIdentity||migrations.bootstrapPolicy!==receipt?.bootstrapPolicy||!['not-applicable','predecessor-reset-lineage-bound-v1'].includes(migrations.resetLineagePolicy)||migrations.directOriginAttestation!==null)fail('migrations.migrated');
      if(!receipt||!exactKeys(receipt,receiptKeys))fail('migration.receipt.shape');else{const resolved=typeof options.resolvePredecessorCheckpoint==='function'?options.resolvePredecessorCheckpoint(receipt.predecessorCheckpointIdentity):null,raw=typeof resolved==='string'?resolved:resolved?.raw,checkpoint=resolved?.checkpoint,predecessor=validateResolvedCheckpoint(checkpoint,raw,receipt.predecessorCheckpointIdentity),validTimes=safe(receipt.appliedAt)&&safe(receipt.predecessorUpdatedAt)&&receipt.predecessorUpdatedAt<=receipt.appliedAt&&safe(receipt.predecessorRevision)&&receipt.predecessorRevision>=1,validText=typeof receipt.source==='string'&&Boolean(receipt.source)&&typeof receipt.predecessorSource==='string'&&Boolean(receipt.predecessorSource)&&validId(receipt.predecessorValidatorId),rebuilt=predecessor&&validatePredecessorContract(predecessor,options.validatePredecessor,options.predecessorValidatorId)&&options.predecessorValidatorId===receipt.predecessorValidatorId?buildActivationTarget(predecessor,{now:receipt.appliedAt,source:receipt.source,predecessorValidatorId:receipt.predecessorValidatorId,checkpoint}):null,rebuiltReceipt=rebuilt?.saveMeta.appliedMigrations.at(-1);if(receipt.from!==13||receipt.to!==14||receipt.receiptVersion!==2||!validTimes||!validText||receipt.appliedAt!==root.activatedAt||receipt.configId!==definitions.configId||receipt.manifestId!==definitions.manifestId||receipt.manifestHash!==manifestHash||!HASH_PATTERN.test(receipt.predecessorSemanticIdentity)||!HASH_PATTERN.test(receipt.predecessorRawIdentity)||!HASH_PATTERN.test(receipt.predecessorCheckpointIdentity)||receipt.predecessorSemanticIdentity!==checkpoint?.semanticIdentity||receipt.predecessorRawIdentity!==checkpoint?.rawIdentity||receipt.predecessorRevision!==predecessor?.saveMeta?.revision||receipt.predecessorUpdatedAt!==predecessor?.saveMeta?.updatedAt||receipt.predecessorSource!==predecessor?.saveMeta?.source||receipt.bootstrapPolicy!==detectBootstrapPolicy(predecessor)||!['direct-schema-13','predecessor-through-migration'].includes(receipt.bootstrapPolicy)||receipt.activeReleaseIds.length!==0||receipt.rewardApplications!==0||receipt.activationTargetIdentity!==rebuiltReceipt?.activationTargetIdentity||receipt.identity!==rebuiltReceipt?.identity||migrations.resetLineagePolicy!==(isObject(predecessor?.saveMeta?.retainedCheckpointLineage)?'predecessor-reset-lineage-bound-v1':'not-applicable'))fail('migration.receipt')}
    }else{
      const lineageKind=migrations?.lineageKind,attestation=migrations?.directOriginAttestation,origin=resolvedDirectOrigin(attestation,options.resolveDirectOrigin),marker=state.saveMeta.retainedCheckpointLineage,currentResetIdentity=marker===undefined?null:sha256(canonicalStringify(marker)),expectedResetPolicy=lineageKind==='safe-reset-schema-14'?'protected-previous-installation-v1':'not-applicable',rebuilt=origin&&validatePredecessorContract(origin,options.validatePredecessor,options.predecessorValidatorId)&&options.predecessorValidatorId===attestation?.predecessorValidatorId&&validateDirectOriginContract(origin,lineageKind,options.validateDirectOrigin,options.directOriginValidatorId)&&options.directOriginValidatorId===attestation?.directOriginValidatorId?buildDirectActivationTarget(origin,{lineageKind,attestation}):null;
      if(receipts.length!==0||migrations.activationReceiptId!==null||migrations.predecessorSemanticIdentity!==null||migrations.predecessorRawIdentity!==null||migrations.predecessorCheckpointIdentity!==null||migrations.bootstrapPolicy!==lineageKind||migrations.resetLineagePolicy!==expectedResetPolicy||!origin||!rebuilt||root.activatedAt!==attestation?.createdAt||state.saveMeta.saveId!==attestation?.saveId||state.saveMeta.createdAt!==attestation?.createdAt||canonicalStringify(rebuilt[ROOT_KEY].migrations)!==canonicalStringify(migrations)||currentResetIdentity!==attestation?.resetLineageIdentity)fail('migrations.direct');
    }
    return{ok:errors.length===0,errors};
  }
  function validateSuccessorState(state,options={}){try{return validateSuccessorStateUnsafe(state,options)}catch{return{ok:false,errors:['validation.exception']}}}

  function rankProjection(rankExp,releasedThrough=5){if(!safe(rankExp)||!safe(releasedThrough)||releasedThrough<1||releasedThrough>30)throw new TypeError('Rank projection input is invalid');let rank=1;for(const row of definitions.rank.table)if(row.rank<=releasedThrough&&rankExp>=row.totalExp)rank=row.rank;const next=rank<releasedThrough?definitions.rank.table[rank]:null;return{rank,rankExp,releasedThrough,capacityThrough:30,atReleasedCap:rank===releasedThrough,bankedBeyondReleasedCap:rank===releasedThrough?Math.max(0,rankExp-definitions.rank.table[releasedThrough-1].totalExp):0,nextTotalExp:next?.totalExp??null,remaining:next?next.totalExp-rankExp:null}}
  function planRankReleaseActivation(state,newReleasedThrough,options={}){
    const currentAuthority=options.currentAuthority||options.authority||definitions,successorAuthority=options.successorAuthority,currentChecked=validateSuccessorState(state,{...options,authority:currentAuthority}),successorChecked=validateReleaseAuthority(successorAuthority);
    if(!currentChecked.ok||!successorChecked.ok||!safe(newReleasedThrough)||newReleasedThrough<=state[ROOT_KEY].ladders.rank.releasedThrough||newReleasedThrough>successorAuthority.rank.capacityThrough)throw new TypeError('Rank release activation input is invalid');
    const oldReleasedThrough=state[ROOT_KEY].ladders.rank.releasedThrough,currentActive=new Set(currentAuthority.releaseManifests.filter(row=>row.active).map(row=>row.id)),newReleases=successorAuthority.releaseManifests.filter(row=>row.active&&!currentActive.has(row.id)),release=newReleases.find(row=>row.releasedRankThrough===newReleasedThrough),newRows=successorAuthority.rank.table.slice(oldReleasedThrough,newReleasedThrough),requiredDependencies=newRows.flatMap(row=>row.contentDependencyIds||[]);
    if(!release||newReleases.some(row=>row.releasedRankThrough>newReleasedThrough)||newRows.length!==newReleasedThrough-oldReleasedThrough||newRows.some((row,index)=>row.rank!==oldReleasedThrough+index+1||!Array.isArray(row.contentDependencyIds)||row.contentDependencyIds.length===0)||new Set(requiredDependencies).size!==requiredDependencies.length||requiredDependencies.some(id=>!validId(id)||!release.contentDependencyIds.includes(id)))throw new TypeError('Rank release lacks its exact content-backed successor manifest');
    const rankExp=state.player?.rankExp;if(!safe(rankExp))throw new TypeError('Player Rank EXP is invalid');const oldProjection=rankProjection(rankExp,oldReleasedThrough),nextProjection=rankProjection(rankExp,newReleasedThrough),acknowledged=new Set(state[ROOT_KEY].ladders.rank.acknowledgedTransitionIds),pending=new Set(state[ROOT_KEY].ladders.rank.pendingTransitionIds),queue=[];
    for(let rank=oldProjection.rank+1;rank<=nextProjection.rank;rank++){const id=`rank.transition.${rank}.v1`,contentDependencyIds=clone(successorAuthority.rank.table[rank-1].contentDependencyIds);if(!acknowledged.has(id)&&!pending.has(id))queue.push({id,rank,contentDependencyIds,rewardApplications:0})}
    return{successorManifestId:successorAuthority.manifestId,successorManifestHash:authorityHash(successorAuthority),releaseId:release.id,oldReleasedThrough,newReleasedThrough,rankExp,oldRank:oldProjection.rank,newRank:nextProjection.rank,queuedTransitions:queue,rewardApplications:0};
  }
  function planFamilyAlignment(state){if(!isObject(state?.family))throw new TypeError('Family state is required');const claimedDefinitionIds=[],readyDefinitionIds=[],mappings=[];for(const familyId of definitions.family.ids){const row=state.family[familyId];if(!isObject(row)||!safe(row.intimacy)||!Array.isArray(row.claimedIntimacyMilestoneIds))throw new TypeError(`Family ${familyId} is invalid`);const markers=new Set(row.claimedIntimacyMilestoneIds);for(const definition of definitions.family.shardMilestones.filter(item=>item.familyId===familyId)){const predecessorClaimed=markers.has(definition.predecessorClaimedMarkerId),eligible=row.intimacy>=definition.threshold;if(predecessorClaimed)claimedDefinitionIds.push(definition.id);else if(eligible)readyDefinitionIds.push(definition.id);mappings.push({familyId,predecessorId:definition.predecessorClaimedMarkerId,targetId:definition.id,threshold:definition.threshold,targetedShards:definition.targetedShards,predecessorClaimed,eligible,disposition:predecessorClaimed?'preserve-claimed-no-grant':eligible?'ready-manual-claim-no-grant-yet':'in-progress',rewardApplications:0})}}return{status:'plan-only-inactive',claimedDefinitionIds:claimedDefinitionIds.sort(),readyDefinitionIds:readyDefinitionIds.sort(),mappings,rewardApplications:0}}
  function nextLegacyTier(progress,claimedCount,thresholds=definitions.legacy.defaultThresholds){if(!safe(progress)||!safe(claimedCount)||!Array.isArray(thresholds)||thresholds.some((value,index)=>!safe(value)||value<1||index>0&&value<=thresholds[index-1]))throw new TypeError('Legacy tier input is invalid');const threshold=thresholds[claimedCount]??null;return{progress,claimedCount,threshold,status:threshold===null?'page-complete':progress>=threshold?'ready':'in-progress',carriedProgress:threshold===null?progress:Math.max(0,progress-threshold)}}
  function bigintGcd(left,right){let a=left<0n?-left:left,b=right<0n?-right:right;while(b){const next=a%b;a=b;b=next}return a}
  function exactNumberRational(value){const view=new DataView(new ArrayBuffer(8));view.setFloat64(0,value,false);const high=view.getUint32(0,false),low=view.getUint32(4,false),exponentBits=(high>>>20)&0x7ff,fraction=(BigInt(high&0xfffff)<<32n)|BigInt(low);let numerator,denominator;if(exponentBits===0){numerator=fraction;denominator=1n<<1074n}else{numerator=(1n<<52n)|fraction;const exponent=exponentBits-1023-52;if(exponent>=0){numerator<<=BigInt(exponent);denominator=1n}else denominator=1n<<BigInt(-exponent)}const divisor=bigintGcd(numerator,denominator);return[numerator/divisor,denominator/divisor]}
  function evaluateAdditiveNumberBase(baseAmount,existingBps,collectionBps,{rounding='nearest'}={}){if(typeof baseAmount!=='number'||!Number.isFinite(baseAmount)||baseAmount<0||baseAmount>Number.MAX_SAFE_INTEGER||!safe(existingBps)||!safe(collectionBps)||!['none','floor','nearest'].includes(rounding))throw new TypeError('Additive number-base input is invalid');const[baseNumerator,baseDenominator]=exactNumberRational(baseAmount),multiplierBps=10000n+BigInt(existingBps)+BigInt(collectionBps),numerator=baseNumerator*multiplierBps,denominator=baseDenominator*10000n,max=BigInt(Number.MAX_SAFE_INTEGER),whole=numerator/denominator,remainder=numerator%denominator;if(whole>max||whole===max&&remainder>0n)throw new RangeError('Additive number-base result exceeds safe integer range');if(rounding==='none'){const value=baseAmount*(1+(existingBps+collectionBps)/10000),[valueNumerator,valueDenominator]=exactNumberRational(value);if(!Number.isFinite(value)||value>Number.MAX_SAFE_INTEGER||valueNumerator*denominator!==numerator*valueDenominator)throw new RangeError('Unrounded additive number-base result is not exactly representable; choose an explicit rounding mode');return{baseAmount,existingBps,collectionBps,numerator:numerator.toString(),denominator:denominator.toString(),value,formula:'exact binary base × (1 + existing bonus + Collection bonus)'}}const rounded=rounding==='nearest'?whole+(remainder*2n>=denominator?1n:0n):whole;if(rounded>max)throw new RangeError('Additive number-base result exceeds safe integer range');return{baseAmount,existingBps,collectionBps,numerator:numerator.toString(),denominator:denominator.toString(),value:Number(rounded),formula:'exact binary base × (1 + existing bonus + Collection bonus)'}}
  function evaluateAdditivePool(baseAmount,existingBps,collectionBps,{rounding='none'}={}){if(!safe(baseAmount)||!safe(existingBps)||!safe(collectionBps)||!['none','floor','nearest'].includes(rounding))throw new TypeError('Additive-pool input is invalid');const multiplierBps=10000n+BigInt(existingBps)+BigInt(collectionBps),numerator=BigInt(baseAmount)*multiplierBps,denominator=10000n,max=BigInt(Number.MAX_SAFE_INTEGER),whole=numerator/denominator,remainder=numerator%denominator;if(whole>max||whole===max&&remainder>0n)throw new RangeError('Additive-pool result exceeds safe integer range');if(rounding==='none'){const divisor=bigintGcd(numerator,denominator),reducedNumerator=numerator/divisor,reducedDenominator=denominator/divisor;if((reducedDenominator&(reducedDenominator-1n))!==0n)throw new RangeError('Unrounded additive result is not exactly representable; choose an explicit rounding mode');const value=Number(whole)+Number(remainder)/10000,[numberNumerator,numberDenominator]=exactNumberRational(value);if(!Number.isFinite(value)||value>Number.MAX_SAFE_INTEGER||numberNumerator*reducedDenominator!==reducedNumerator*numberDenominator)throw new RangeError('Unrounded additive result is not exactly representable; choose an explicit rounding mode');return{baseAmount,existingBps,collectionBps,numerator:numerator.toString(),denominator:10000,value,formula:'base × (1 + existing bonus + Collection bonus)'}}const rounded=rounding==='nearest'?whole+(remainder*2n>=denominator?1n:0n):whole;if(rounded>max)throw new RangeError('Additive-pool result exceeds safe integer range');return{baseAmount,existingBps,collectionBps,numerator:numerator.toString(),denominator:10000,value:Number(rounded),formula:'base × (1 + existing bonus + Collection bonus)'}}
  const formulaOrder=freeze({fellowPower:['base-before-final-pool','one-plus-might-bps-plus-collection-power-bps','round-once'],buildingEarnings:['structural-earnings','one-plus-oath-bps-plus-collection-earnings-bps'],eligibleExp:['raw-earned-exp','one-plus-authored-exp-bps-plus-collection-exp-bps','explicit-settlement-rounding'],facilityActiveReward:['base-active-reward','one-plus-authored-active-bps-plus-local-collection-bps','explicit-settlement-rounding']});
  const api=freeze({version:2,configId:definitions.configId,status:'inactive-foundation-only',rootKey:ROOT_KEY,successorSchema:SUCCESSOR_SCHEMA,migrationId:MIGRATION_ID,recentGrantLimit:RECENT_GRANT_LIMIT,grantFoldBatch:GRANT_FOLD_BATCH,definitions,manifestHash,formulaOrder,canonicalStringify,sha256,authorityHash,releaseSetIdentity,releaseManifestHash,validateDefinitions,validateReleaseAuthority,validateSuccessorState,currentSchema13Projection,detectBootstrapPolicy,capturePreSuccessorCheckpoint,attestPreSuccessorCheckpoint,captureDirectOriginAttestation,createDirectSchema14,migrateSchema13To14,createInactiveRoot,emptyCollectionTotals,collectionTotals,validateCollectionDefinition,captureCollectionGrantPayload,previewCollectionGrant,finalizeCollectionGrant,foldCollectionReceiptsInState,rankProjection,planRankReleaseActivation,planFamilyAlignment,nextLegacyTier,evaluateAdditiveNumberBase,evaluateAdditivePool});
  Object.defineProperty(global,'EVERSTEAD_PHASE24C_FOUNDATION',{configurable:false,enumerable:false,writable:false,value:api});
})(globalThis);
