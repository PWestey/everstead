# Phase 22C independent actual-DOM gate

This QA-only harness loads the frozen production candidate at `ee2ea1e73660562ba735d92e1d715b073c37b1eb` into isolated in-memory storage. It uses only the accepted Phase 16, Phase 18/19, and Phase 20/21 QA bridges for deterministic fixture construction, then opens every surface through its real Village hotspot.

Serve the repository root over HTTP and open:

`qa/phase-22c-independent/index.html`

The active matrix covers 320×568, 390×844, 430×932, 768×1024, 1024×768, 130% root copy, and reduced motion. An additional inactive-production realm confirms that no QA bridge or Phase 22C facility surface is exposed without isolated destructive authorization.

Actual production DOM coverage includes:

- Restaurant claim-ready truth, canonical pending-offer Gold, fail-closed Sale/Tip detail, and unlock-filtered local stock.
- Apothecary Recheck and claim-ready case summaries.
- Schoolhouse lesson and Graduation readiness, exact rendering of the isolated QA fixture’s Gold-only Graduation offer, local Education, and development domains. The companion focused gate freezes the full production-policy Graduation reward kinds and locked-seat requirement copy; the accepted Phase 18/19 bridge intentionally forces its synthetic policy, whose graduation fixture has maximum seat capacity and does not invent Gifts or Relic Stones.
- Claim-ready sheets for all eight Phase 20/21 successor facilities with exact captured Gold and local progress.
- Market & Workshop reservation/stock, Training Grounds participants, Gardens growth, and an exact-once receipt summary built by an allowed pre-baseline bridge claim.

Fixture construction is explicitly outside the measured traversal. After each real sheet is open, the harness establishes the raw-save, revision, write-count, resources, local progress, and reward baseline. It then performs only DOM inspection, resize, scroll, and Escape. Those measured actions must produce zero writes and preserve raw bytes, revision, resources, local progress, and reward/claim state exactly. No claim control is clicked during a measured traversal.

Every surface also proves 44×44 visible controls (including disabled controls), readable disabled state, keyboard focus entry and exact Escape return, no horizontal overflow, and uncovered actionable controls. Claim-ready and in-progress records exercise the sticky action dock; the receipt-only state honestly exercises its production sheet footer because it has no pending record action. The gate waits for the existing migration toast to be hidden before geometry and hit testing. It also verifies reduced motion, exact source/load identity, and zero native-storage access or warning/error console output.

Run the static verifier from the repository root:

```sh
node qa/phase-22c-independent/verify.mjs
```
