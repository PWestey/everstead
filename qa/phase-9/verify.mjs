import {readFileSync} from 'node:fs';
import {resolve} from 'node:path';
import {createHash} from 'node:crypto';
import vm from 'node:vm';
const repoRoot=resolve(new URL('../..',import.meta.url).pathname);
const phaseSevenVerifier=readFileSync(resolve(repoRoot,'qa/phase-7/verify.mjs'),'utf8');
let harnessModuleSource=phaseSevenVerifier.split('const suite=String.raw`')[0].replace("const repoRoot=resolve(new URL('../..',import.meta.url).pathname);",`const repoRoot=${JSON.stringify(repoRoot)};`)+'\nexport {harness};\n';
const {harness:phaseSevenHarness}=await import('data:text/javascript;base64,'+Buffer.from(harnessModuleSource).toString('base64'));
let harness=phaseSevenHarness
 .replace("  preV8:'oathforge_new_world_proto_v01__raw_backup_v7',\n  staging:","  preV8:'oathforge_new_world_proto_v01__raw_backup_v7',\n  preV9:'oathforge_new_world_proto_v01__raw_backup_v8',\n  preV10:'oathforge_new_world_proto_v01__raw_backup_v9',\n  staging:")
 .replace('preV8BackupRaw:keys.preV8,stagingRaw:','preV8BackupRaw:keys.preV8,preV9BackupRaw:keys.preV9,preV10BackupRaw:keys.preV10,stagingRaw:')
 .replace('return value?.schemaVersion===8?value:null','return value?.schemaVersion===10?value:null')
 .replace('    tamperClear(){',`    p9:Object.freeze({state:()=>clone(S),valid:value=>validation(value,10),config:()=>clone({identity:PHASE_NINE_CONFIG_IDENTITY,player:PLAYER_CONFIG,unlocks:PLAYER_UNLOCK_DEFS,ranks:PLAYER_RANK_DEFS,gates:PHASE_NINE_GATE_IDS}),rankFor:playerRankForExp,rankProgress:playerRankProgress,unlock:playerUnlockState,stageAccess:playerStageAccess,routeAccess:playerRouteAccess,diagnostics:persistenceDiagnostics,export:safePersistenceExport,blocked:()=>clone(PERSISTENCE_BLOCKED),preview:campaignPreview,run:runFellowCampaign,select:selectFellowCampaignStage,route:setAdventure,named:(name,value)=>{try{return qaNamedAction(name,value)}catch(error){return{ok:false,error:String(error.message||error)}}},grant:(resource,amount,id)=>{try{return{ok:true,state:qaGrant({resource,amount,...(id?{id}:{})})}}catch(error){return{ok:false,error:String(error.message||error)}}},profile:openPlayerProfile,adventure:adventureScreen,more:moreScreen,top:topbar,reset:()=>persistenceAction('safe-reset'),rankTransition:(before,after)=>phaseNineRankTransition(before,after),fixtureProbe(input){try{return{ok:true,state:qaInstallFixture(input)}}catch(error){return{ok:false,error:String(error.message||error)}}},tamperUnlock(kind){const copy=clone(S),ids=copy.playerUnlocks.grandfatheredUnlockIds;if(kind==='missing')ids.pop();else if(kind==='extra')ids.push('foreign');else if(kind==='duplicate')ids.push(ids[0]);else if(kind==='reordered')ids.reverse();else if(kind==='config')copy.playerUnlocks.configIdentity='foreign';else if(kind==='partial')copy.playerUnlocks.grandfatheredUnlockIds=ids.slice(0,2);return validation(copy,10)},tamperReceiptOrder(){const copy=clone(S),receipt=copy.saveMeta.appliedMigrations.pop();copy.saveMeta.appliedMigrations.unshift(receipt);return validation(copy,10)},tamperStripMigration(){const copy=clone(S);copy.saveMeta.appliedMigrations=copy.saveMeta.appliedMigrations.filter(item=>item?.id!=='schema-9-to-10');copy.playerUnlocks.grandfatheredUnlockIds=[];return validation(copy,10)},tamperStripMigrationRaw(){const copy=clone(S);copy.saveMeta.appliedMigrations=copy.saveMeta.appliedMigrations.filter(item=>item?.id!=='schema-9-to-10');copy.playerUnlocks.grandfatheredUnlockIds=[];return JSON.stringify(copy)},tamperStage(raw,baseRaw,field){const envelope=JSON.parse(raw),state=clone(envelope.state);if(field==='gold')state.gold+=777;else if(field==='gifts')state.gifts+=1;else throw new Error('Unknown tamper field');return{stateRaw:JSON.stringify(state),stagingRaw:JSON.stringify(stagingEnvelope(state,baseRaw,envelope.source))}},selectWithToast(id){const result=selectFellowCampaignStage(id,{present:true});return{result,toast:document.querySelector('#toast')?.innerHTML??''}},schemaNineProjection(route){const state=clone(S);delete state.playerUnlocks;state.schemaVersion=9;state.ui.adventure=route;state.saveMeta.appliedMigrations=state.saveMeta.appliedMigrations.filter(item=>item?.id!=='schema-9-to-10');delete state.saveMeta.retainedCheckpointLineage;return JSON.stringify(state)},schemaNineDefault(){const at=1787853600000,meta=newMetadata('fresh',at,[]);return JSON.stringify(schemaScopedDefaultState(meta,9))},schemaScoped:(meta,version)=>JSON.stringify(schemaScopedDefaultState(meta,version)),runtime:()=>qaRuntimeSnapshot()}),
    tamperClear(){`);
const suite=String.raw`
const p9=(run,expression)=>internal(run,'p9.'+expression),slotImage=run=>JSON.stringify(Object.fromEntries(run.slots));
const smokeRows=[],assertRow=(id,pass,detail='')=>smokeRows.push({id,pass:Boolean(pass),detail});
const fresh=runRealm(freshOptions),state=active(fresh);
const config=p9(fresh,'config()'),expectedUnlocks=[['fellow-campaign-replay',2,'campaign-action','replay',false],['fellow-campaign-stage-3',2,'campaign-stage','broken-roads-3',true],['fellow-campaign-stage-4',2,'campaign-stage','broken-roads-4',true],['companion-campaign',2,'adventure-route','companionCampaign',true],['fellow-campaign-stage-5',3,'campaign-stage','broken-roads-5',true],['fellow-campaign-stage-6',3,'campaign-stage','broken-roads-6',true],['companion-tower',3,'adventure-route','companionTower',true],['fellow-campaign-stage-7',4,'campaign-stage','broken-roads-7',true],['fellow-campaign-stage-8',4,'campaign-stage','broken-roads-8',true],['fellow-campaign-stage-9',4,'campaign-stage','broken-roads-9',true],['fellow-expedition',4,'adventure-route','fellowExpedition',true],['fellow-campaign-stage-10',5,'campaign-stage','broken-roads-10',true]];
assertRow('config-identity',config.identity==='phase-9-player-rank-unlocks-v1',config.identity);assertRow('rank-thresholds-exact',JSON.stringify(config.player.rankThresholds)===JSON.stringify([0,50,125,225,350]));assertRow('rank-cap-exact',config.player.rankCap===5);assertRow('unlock-count-exact',config.unlocks.length===12);assertRow('gate-count-exact',config.gates.length===11);assertRow('gate-order-exact',JSON.stringify(config.gates)===JSON.stringify(expectedUnlocks.filter(item=>item[4]).map(item=>item[0])));
for(const [index,expected] of expectedUnlocks.entries()){const actual=config.unlocks[index];assertRow('unlock-definition-'+(index+1),actual.id===expected[0]&&actual.rank===expected[1]&&actual.targetKind===expected[2]&&actual.targetKey===expected[3]&&actual.phaseNineGate===expected[4],actual)}
for(const [exp,rank] of [[0,1],[1,1],[49,1],[50,2],[51,2],[124,2],[125,3],[126,3],[224,3],[225,4],[226,4],[349,4],[350,5],[351,5],[Number.MAX_SAFE_INTEGER,5]])assertRow('rank-boundary-'+exp,p9(fresh,'rankFor('+exp+')')===rank,p9(fresh,'rankFor('+exp+')'));
assertRow('fresh-boot',fresh.thrown===null&&internal(fresh,'runtime().blocked')===null&&state?.schemaVersion===10,fresh.thrown?.stack||internal(fresh,'runtime().blocked'));
assertRow('fresh-empty-grandfather',JSON.stringify(state?.playerUnlocks?.grandfatheredUnlockIds)==='[]');
assertRow('fresh-export-12',Object.keys(p9(fresh,'export().readErrors')).length===12);
const adventureHtml=p9(fresh,'adventure()'),moreHtml=p9(fresh,'more()'),topHtml=p9(fresh,'top()');assertRow('fresh-locked-tabs-visible',adventureHtml.includes('Companions · R2')&&adventureHtml.includes('Tower · R3')&&adventureHtml.includes('Expedition · R4')&&adventureHtml.includes('aria-disabled="true"'));assertRow('profile-entrypoints',topHtml.includes('data-player-profile')&&moreHtml.includes('data-player-profile'));
const diagnostics=p9(fresh,'diagnostics()');assertRow('registry-exact-counts',diagnostics.playerRank.unlockDefinitions.length===12&&diagnostics.playerRank.unlockDefinitions.filter(item=>item.phaseNineGate).length===11);assertRow('rank-source-vector',JSON.stringify(diagnostics.playerRank.rankExpSources[0].values)===JSON.stringify([25,30,35,40,45,50,55,60,65,70]));
const freshStageThreeNode=adventureHtml.match(/<button[^>]*data-campaign-stage="broken-roads-3"[^>]*>[\s\S]*?<\/button>/)?.[0]||'';
assertRow('fresh-locked-stage-visible',freshStageThreeNode.includes('data-rank-required="2"')&&freshStageThreeNode.includes('Rank 2'));
assertRow('fresh-locked-stage-focusable',freshStageThreeNode.includes('aria-disabled="true"')&&!/\sdisabled(?:\s|>)/.test(freshStageThreeNode));
assertRow('campaign-button-valid-attributes',freshStageThreeNode.includes('rank-locked')&&!freshStageThreeNode.includes('class="campaign-node rank-locked" aria-disabled="true" class='));
const lockedBefore=slotImage(fresh),route=p9(fresh,"route('companionCampaign',{present:false})"),stage=p9(fresh,"select('broken-roads-3',{present:false})");
assertRow('locked-route-zero-write',route===false&&slotImage(fresh)===lockedBefore);
assertRow('locked-stage-zero-write',stage===false&&slotImage(fresh)===lockedBefore);
for(const [name,payload] of [['companion-campaign-select',{id:'mosslight-1'}],['companion-campaign-run',{id:'mosslight-1'}],['companion-tower-clear',{}],['companion-tower-claim',{}],['fellow-expedition-push',{}],['fellow-expedition-claim',{}]]){const before=slotImage(fresh),revision=active(fresh).saveMeta.revision,result=p9(fresh,'named('+JSON.stringify(name)+','+JSON.stringify(payload)+')');assertRow('rank-locked-'+name,!result.ok&&result.changed===false&&slotImage(fresh)===before&&active(fresh).saveMeta.revision===revision,result.error)}
const futureClick=p9(fresh,"selectWithToast('broken-roads-10')");assertRow('sequence-toast-outranks-rank',futureClick.result===false&&futureClick.toast==='Clear the prior Campaign stage first.',futureClick);
const one=p9(fresh,"run('broken-roads-1',{confirmed:true,present:false})"),two=p9(fresh,"run('broken-roads-2',{confirmed:true,present:false})"),afterTwo=active(fresh);
assertRow('rank2-after-two',one.ok&&two.ok&&afterTwo.player.rank===2&&afterTwo.player.rankExp===55,afterTwo.player);
const freshTransition=p9(fresh,'rankTransition(1,2).newlyRankUnlockedIds');assertRow('fresh-rank-transition-effective-delta',JSON.stringify(freshTransition)===JSON.stringify(['fellow-campaign-replay','fellow-campaign-stage-3','fellow-campaign-stage-4','companion-campaign']),freshTransition);
assertRow('route-opens-rank2',p9(fresh,"route('companionCampaign',{present:false})").ok===true);
assertRow('rank2-replay-open',p9(fresh,"unlock('fellow-campaign-replay').effectiveAccess")===true);assertRow('rank2-stage3-open',p9(fresh,"stageAccess('broken-roads-3').effectiveAccess")===true);assertRow('rank2-stage5-locked',p9(fresh,"stageAccess('broken-roads-5').effectiveAccess")===false);
assertRow('qa-gold-credit',p9(fresh,"grant('gold',5000000)").ok===true);for(const id of ['cael','lyra','orin','selene','rook','mira'])assertRow('qa-fellow-power-'+id,p9(fresh,"grant('fellowExp',1000000,'"+id+"')").ok===true);
for(const stage of [3,4])assertRow('first-clear-stage-'+stage,p9(fresh,"run('broken-roads-"+stage+"',{confirmed:true,present:false})").ok===true);assertRow('rank3-after-four',active(fresh).player.rank===3&&active(fresh).player.rankExp===130,active(fresh).player);assertRow('rank3-tower-open',p9(fresh,"routeAccess('companionTower').effectiveAccess")===true);assertRow('rank3-stage7-locked',p9(fresh,"stageAccess('broken-roads-7').effectiveAccess")===false);
for(const stage of [5,6])assertRow('first-clear-stage-'+stage,p9(fresh,"run('broken-roads-"+stage+"',{confirmed:true,present:false})").ok===true);assertRow('rank4-after-six',active(fresh).player.rank===4&&active(fresh).player.rankExp===225,active(fresh).player);assertRow('rank4-expedition-open',p9(fresh,"routeAccess('fellowExpedition').effectiveAccess")===true);assertRow('rank4-stage10-locked',p9(fresh,"stageAccess('broken-roads-10').effectiveAccess")===false);
for(const stage of [7,8,9])assertRow('first-clear-stage-'+stage,p9(fresh,"run('broken-roads-"+stage+"',{confirmed:true,present:false})").ok===true);assertRow('rank5-after-nine',active(fresh).player.rank===5&&active(fresh).player.rankExp===405,active(fresh).player);assertRow('rank5-stage10-open',p9(fresh,"stageAccess('broken-roads-10').effectiveAccess")===true);assertRow('first-clear-stage-10',p9(fresh,"run('broken-roads-10',{confirmed:true,present:false})").ok===true);assertRow('rank-exp-total-475',active(fresh).player.rank===5&&active(fresh).player.rankExp===475,active(fresh).player);assertRow('phase8-receipts-preserved',active(fresh).fellowCampaign.lastReceipt.rewardIdentityVersion===2&&active(fresh).relicProgressLedger.lastCampaignReceipt.identityVersion===1);
const schemaNineRaw=p9(fresh,'schemaNineDefault()'),migration=runRealm({...freshOptions,activeRaw:schemaNineRaw,now:1787853601000}),migrated=active(migration);
assertRow('schema9-migrates',migrated?.schemaVersion===10&&internal(migration,'runtime().blocked')===null,internal(migration,'runtime().blocked'));
assertRow('migration-full-grandfather',migrated?.playerUnlocks?.grandfatheredUnlockIds?.length===11,migrated?.playerUnlocks);
assertRow('migration-pre-v10-exact',migration.slots.get(keys.preV10)===schemaNineRaw);
for(const kind of ['missing','extra','duplicate','reordered','config','partial'])assertRow('grandfather-forgery-'+kind,p9(migration,"tamperUnlock('"+kind+"').ok")===false);
for(const id of config.gates){const access=p9(migration,"unlock('"+id+"')");assertRow('migrated-grandfather-access-'+id,access.grandfatherAccess&&access.effectiveAccess&&!access.rankAccess,access)}
const migratedTransition=p9(migration,'rankTransition(1,2).newlyRankUnlockedIds'),strippedMigrationRaw=p9(migration,'tamperStripMigrationRaw()'),strippedMigrationSlots=Object.fromEntries(migration.slots);strippedMigrationSlots[keys.active]=strippedMigrationRaw;const strippedMigration=runRealm({initialSlots:strippedMigrationSlots,now:1787853602500});assertRow('grandfather-rank-transition-no-announcement',JSON.stringify(migratedTransition)===JSON.stringify(['fellow-campaign-replay']),migratedTransition);
assertRow('stripped-migration-not-fresh',internal(strippedMigration,'runtime().blocked')!==null&&strippedMigration.slots.get(keys.active)===strippedMigrationRaw,internal(strippedMigration,'runtime().blocked'));
const grandfatherRoute=p9(migration,"route('companionTower',{present:false})");assertRow('grandfather-route-mutates',grandfatherRoute.ok===true&&active(migration).ui.adventure==='companionTower');
const reload=runRealm({initialSlots:Object.fromEntries(migration.slots),now:1787853602000});
assertRow('migration-reload',active(reload)?.schemaVersion===10&&internal(reload,'runtime().blocked')===null,internal(reload,'runtime().blocked'));
const authenticatedMissing=runRealm({...freshOptions,activeRaw:null,backupRaw:schemaNineRaw,preV10BackupRaw:schemaNineRaw,now:1787853603000});assertRow('released-fresh-missing-recovery',active(authenticatedMissing)?.schemaVersion===10&&internal(authenticatedMissing,'runtime().blocked')===null,internal(authenticatedMissing,'runtime().blocked'));
const authenticatedActive=runRealm({...freshOptions,activeRaw:schemaNineRaw,backupRaw:schemaNineRaw,now:1787853604000});assertRow('released-fresh-active-with-backup',active(authenticatedActive)?.schemaVersion===10&&internal(authenticatedActive,'runtime().blocked')===null,internal(authenticatedActive,'runtime().blocked'));
const protectedReadNames=['active','staging','backup','pre-v2','pre-v3','pre-v4','pre-v5','pre-v6','pre-v7','pre-v8','pre-v9','pre-v10'];for(const name of protectedReadNames)for(const suffix of ['read','read-verify']){const run=runRealm({...freshOptions,activeRaw:schemaNineRaw,now:1787853604100,fault:{enabled:true,operation:'getItem',step:'schema-10-'+name+'-'+suffix}});assertRow('protected-'+name+'-'+suffix+'-triggered',run.fault.remaining===0);assertRow('protected-'+name+'-'+suffix+'-zero-write',writes(run)===0&&run.slots.get(keys.active)===schemaNineRaw&&internal(run,'runtime().blocked')!==null,internal(run,'runtime().blocked'))}
const migrationFaultHooks=[['pre-v10-backup-write','setItem'],['pre-v10-backup-verify','getItem'],['pre-v10-backup-verify-reread','getItem'],['staging-new-owner','getItem'],['staging-write','setItem'],['staging-verify','getItem'],['active-conflict-check','getItem'],['active-write','setItem'],['active-verify','getItem'],['staging-cleanup-owner','getItem'],['staging-cleanup','removeItem'],['staging-cleanup-verify','getItem']];for(const [step,operation] of migrationFaultHooks){const interrupted=runRealm({...freshOptions,activeRaw:schemaNineRaw,now:1787853604200,fault:{enabled:true,operation,step}}),retry=runRealm({initialSlots:Object.fromEntries(interrupted.slots),now:1787853604300}),value=active(retry);assertRow('migration-fault-'+step+'-triggered',interrupted.fault.remaining===0,interrupted.persistenceLog);assertRow('migration-fault-'+step+'-retry',value?.schemaVersion===10&&internal(retry,'runtime().blocked')===null,internal(retry,'runtime().blocked'));assertRow('migration-fault-'+step+'-one-receipt',value?.saveMeta?.appliedMigrations?.filter(item=>item.id==='schema-9-to-10').length===1)}
const interruptedMigration=runRealm({...freshOptions,activeRaw:schemaNineRaw,now:1787853605000,fault:{enabled:true,operation:'setItem',step:'active-write'}}),interruptedSlots=Object.fromEntries(interruptedMigration.slots);delete interruptedSlots[keys.active];const missingActiveStage=runRealm({initialSlots:interruptedSlots,now:1787853606000});assertRow('pending-direct-migration-missing-active',active(missingActiveStage)?.schemaVersion===10&&internal(missingActiveStage,'runtime().blocked')===null,internal(missingActiveStage,'runtime().blocked'));
const directPendingTamper=p9(interruptedMigration,'tamperStage('+JSON.stringify(interruptedMigration.slots.get(keys.staging))+','+JSON.stringify(schemaNineRaw)+',"gold")'),directPendingSlots=Object.fromEntries(interruptedMigration.slots);directPendingSlots[keys.staging]=directPendingTamper.stagingRaw;const directPending=runRealm({initialSlots:directPendingSlots,now:1787853606100});assertRow('forged-direct-pending-active-present-blocked',internal(directPending,'runtime().blocked')?.kind==='staging-provenance'&&directPending.slots.get(keys.staging)===directPendingTamper.stagingRaw,internal(directPending,'runtime().blocked'));
const directMissingSlots={...directPendingSlots};delete directMissingSlots[keys.active];const directMissing=runRealm({initialSlots:directMissingSlots,now:1787853606200});assertRow('forged-direct-pending-active-missing-blocked',internal(directMissing,'runtime().blocked')?.kind==='staging-provenance'&&directMissing.slots.get(keys.staging)===directPendingTamper.stagingRaw,internal(directMissing,'runtime().blocked'));
const directCommittedInterrupted=runRealm({...freshOptions,activeRaw:schemaNineRaw,now:1787853606300,fault:{enabled:true,operation:'getItem',step:'active-verify'}}),directCommittedTamper=p9(directCommittedInterrupted,'tamperStage('+JSON.stringify(directCommittedInterrupted.slots.get(keys.staging))+','+JSON.stringify(schemaNineRaw)+',"gifts")'),directCommittedSlots=Object.fromEntries(directCommittedInterrupted.slots);directCommittedSlots[keys.active]=directCommittedTamper.stateRaw;directCommittedSlots[keys.staging]=directCommittedTamper.stagingRaw;const directCommitted=runRealm({initialSlots:directCommittedSlots,now:1787853606400});assertRow('forged-direct-committed-blocked',internal(directCommitted,'runtime().blocked')?.kind==='staging-provenance'&&directCommitted.slots.get(keys.active)===directCommittedTamper.stateRaw&&directCommitted.slots.get(keys.staging)===directCommittedTamper.stagingRaw,internal(directCommitted,'runtime().blocked'));
const recoveryPendingInterrupted=runRealm({...freshOptions,activeRaw:null,backupRaw:schemaNineRaw,preV10BackupRaw:schemaNineRaw,now:1787853606500,fault:{enabled:true,operation:'setItem',step:'active-write'}}),recoveryPendingTamper=p9(recoveryPendingInterrupted,'tamperStage('+JSON.stringify(recoveryPendingInterrupted.slots.get(keys.staging))+',null,"gold")'),recoveryPendingSlots=Object.fromEntries(recoveryPendingInterrupted.slots);recoveryPendingSlots[keys.staging]=recoveryPendingTamper.stagingRaw;const recoveryPending=runRealm({initialSlots:recoveryPendingSlots,now:1787853606600});assertRow('forged-recovery-pending-blocked',internal(recoveryPending,'runtime().blocked')?.kind==='staging-provenance'&&recoveryPending.slots.get(keys.staging)===recoveryPendingTamper.stagingRaw,internal(recoveryPending,'runtime().blocked'));
const recoveryCommittedInterrupted=runRealm({...freshOptions,activeRaw:null,backupRaw:schemaNineRaw,preV10BackupRaw:schemaNineRaw,now:1787853606700,fault:{enabled:true,operation:'getItem',step:'active-verify'}}),recoveryCommittedTamper=p9(recoveryCommittedInterrupted,'tamperStage('+JSON.stringify(recoveryCommittedInterrupted.slots.get(keys.staging))+',null,"gifts")'),recoveryCommittedSlots=Object.fromEntries(recoveryCommittedInterrupted.slots);recoveryCommittedSlots[keys.active]=recoveryCommittedTamper.stateRaw;recoveryCommittedSlots[keys.staging]=recoveryCommittedTamper.stagingRaw;const recoveryCommitted=runRealm({initialSlots:recoveryCommittedSlots,now:1787853606800});assertRow('forged-recovery-committed-blocked',internal(recoveryCommitted,'runtime().blocked')?.kind==='staging-provenance'&&recoveryCommitted.slots.get(keys.active)===recoveryCommittedTamper.stateRaw&&recoveryCommitted.slots.get(keys.staging)===recoveryCommittedTamper.stagingRaw,internal(recoveryCommitted,'runtime().blocked'));
const prefixRun=runRealm(freshOptions);p9(prefixRun,"grant('gold',5000000)");for(const id of ['cael','lyra','orin','selene','rook','mira'])p9(prefixRun,"grant('fellowExp',1000000,'"+id+"')");const routes=['fellowCampaign','fellowExpedition','companionCampaign','companionTower'];for(let prefix=0;prefix<=9;prefix++){if(prefix>0)assertRow('prefix-source-clear-'+prefix,p9(prefixRun,"run('broken-roads-"+prefix+"',{confirmed:true,present:false})").ok===true);for(const routeId of routes){const predecessorRaw=p9(prefixRun,"schemaNineProjection('"+routeId+"')"),predecessor=JSON.parse(predecessorRaw),candidate=runRealm({...freshOptions,activeRaw:predecessorRaw,now:1787853607000+prefix}),value=active(candidate);assertRow('schema9-prefix-'+prefix+'-route-'+routeId,value?.schemaVersion===10&&internal(candidate,'runtime().blocked')===null&&value.player.rankExp===predecessor.player.rankExp&&value.gold===predecessor.gold&&value.ui.adventure===routeId&&JSON.stringify(value.fellowCampaign.clearedStageIds)===JSON.stringify(predecessor.fellowCampaign.clearedStageIds)&&value.playerUnlocks.grandfatheredUnlockIds.length===11,internal(candidate,'runtime().blocked'))}}
assertRow('prefix-source-clear-10',p9(prefixRun,"run('broken-roads-10',{confirmed:true,present:false})").ok===true);const completedNineRaw=p9(prefixRun,"schemaNineProjection('fellowCampaign')"),completedNine=runRealm({...freshOptions,activeRaw:completedNineRaw,now:1787853608000});assertRow('schema9-completed-prefix-10',active(completedNine)?.player.rank===5&&active(completedNine)?.player.rankExp===475&&active(completedNine)?.fellowCampaign.clearedStageIds.length===10&&active(completedNine)?.playerUnlocks.grandfatheredUnlockIds.length===11,internal(completedNine,'runtime().blocked'));
for(const [label,raw] of [['legacy',legacyRaw],['schema1',schemaOneRaw]]){const run=runRealm({...freshOptions,activeRaw:raw,now:1787853610000}),value=active(run),again=runRealm({initialSlots:Object.fromEntries(run.slots),now:1787853611000});assertRow(label+'-to-schema10',value?.schemaVersion===10&&internal(run,'runtime().blocked')===null,internal(run,'runtime().blocked'));assertRow(label+'-pre-v10-schema9',JSON.parse(run.slots.get(keys.preV10)).schemaVersion===9);assertRow(label+'-reload',active(again)?.schemaVersion===10&&internal(again,'runtime().blocked')===null,internal(again,'runtime().blocked'));if(label==='schema1'){assertRow('phase-nine-receipt-must-be-last',p9(run,'tamperReceiptOrder().ok')===false);assertRow('stripped-historical-migration-invalid',p9(run,'tamperStripMigration().ok')===false)}}
const resetRun=runRealm({initialSlots:Object.fromEntries(migration.slots),now:1787853620000});p9(resetRun,'reset()');const resetState=active(resetRun),resetReload=runRealm({initialSlots:Object.fromEntries(resetRun.slots),now:1787853621000});assertRow('safe-reset-marker5',resetState?.saveMeta?.retainedCheckpointLineage?.version===5&&resetState.playerUnlocks.grandfatheredUnlockIds.length===0,internal(resetRun,'runtime().blocked'));assertRow('safe-reset-reload',active(resetReload)?.schemaVersion===10&&internal(resetReload,'runtime().blocked')===null,internal(resetReload,'runtime().blocked'));
for(const name of protectedReadNames){const run=runRealm(freshOptions),before=slotImage(run),writesBefore=writes(run);Object.assign(run.fault,{enabled:true,operation:'getItem',step:'schema-10-'+name+'-read',remaining:1,skip:0});p9(run,'reset()');assertRow('safe-reset-read-'+name+'-triggered',run.fault.remaining===0);assertRow('safe-reset-read-'+name+'-zero-write',writes(run)===writesBefore&&slotImage(run)===before)}
const fixtureTarget=runRealm(freshOptions);p9(fixtureTarget,"grant('gold',1)");const fixtureTargetRaw=fixtureTarget.slots.get(keys.active),fixturePayload={activeRaw:fixtureTargetRaw,backupRaw:null,preV2BackupRaw:null,preV3BackupRaw:null,preV4BackupRaw:null,preV5BackupRaw:null,preV6BackupRaw:null,preV7BackupRaw:null,preV8BackupRaw:null,preV9BackupRaw:null,preV10BackupRaw:null,stagingRaw:null},fixtureRun=runRealm(freshOptions),fixtureBefore=slotImage(fixtureRun),fixtureRuntime=p9(fixtureRun,'runtime()');for(const [slotName,slotKey] of Object.entries(keys)){Object.assign(fixtureRun.fault,{enabled:true,operation:'getItem',key:slotKey,step:null,remaining:1,skip:2,adapterOnly:true});const result=p9(fixtureRun,'fixtureProbe('+JSON.stringify(fixturePayload)+')'),slotsRestored=slotImage(fixtureRun)===fixtureBefore,afterRuntime=p9(fixtureRun,'runtime()'),runtimeKeys=Object.keys(fixtureRuntime).filter(key=>key!=='logLength'),runtimeRestored=runtimeKeys.every(key=>JSON.stringify(afterRuntime[key])===JSON.stringify(fixtureRuntime[key])),runtimeDiff=runtimeKeys.filter(key=>JSON.stringify(afterRuntime[key])!==JSON.stringify(fixtureRuntime[key]));assertRow('fixture-postboot-'+slotName+'-triggered',fixtureRun.fault.remaining===0&&!result.ok,result.error);assertRow('fixture-postboot-'+slotName+'-rollback',slotsRestored&&runtimeRestored,'slots='+slotsRestored+' runtime='+runtimeRestored+' diff='+runtimeDiff.join(',')+' '+result.error)}
const eventRun=runRealm(freshOptions),staleBefore=internal(eventRun,'runtime().stale');for(const listener of eventRun.listeners.storage??[])listener({storageArea:{},key:keys.preV10});const staleForeign=internal(eventRun,'runtime().stale');for(const listener of eventRun.listeners.storage??[])listener({storageArea:null,key:keys.preV10});const staleNull=internal(eventRun,'runtime().stale');for(const listener of eventRun.listeners.storage??[])listener({storageArea:eventRun.context.localStorage,key:keys.preV10});assertRow('pre-v10-event-foreign-ignored',staleBefore===false&&staleForeign===false&&staleNull===false);assertRow('pre-v10-event-native-stale',internal(eventRun,'runtime().stale')===true);
console.log(JSON.stringify({total:smokeRows.length,passed:smokeRows.filter(row=>row.pass).length,failures:smokeRows.filter(row=>!row.pass)},null,2));
if(smokeRows.some(row=>!row.pass))process.exitCode=1;
`;
eval(harness+suite);
