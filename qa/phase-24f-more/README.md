# Phase 24F current-schema More gate

This additive successor gate protects the Phase 24F consolidation of the schema-14 More screen into one presentation owner. It does not change progression, economy, persistence, recovery semantics, or public-release scope.

The static verifier removes the two explicit Phase 24F blocks—its narrowly scoped accessibility style loader and More ownership—and requires the remaining `index.html` bytes to equal exact accepted Phase 24E commit `dc4f038d0553ea79cdae3b096f10e2cb70494f56`. It also freezes the Phase 24E evidence package, verifies the one direct More assignment, and rejects a new wrapper or binder chain.

The Chromium matrix reuses Phase 24D's production-action seed builder. Fresh, public-established, foundation-thin, and public high-investment saves run on ordinary non-QA URLs at 320×568 and 390×844. Every journey proves:

- one schema-14 owner renders exactly one More main with deterministic, unique section order;
- current public profile, Wayfarer, Codex, story, Scaling Authority, Campaign Guidance, Preferences, Save & Recovery, save-health, and migration-history content is retained according to save state;
- the private Phase 17 More card remains excluded from the public build;
- safe modal previews open once and cancel without executing downloads, imports, restores, forgets, claims, or reset confirmation;
- More rendering, modal previews, details, route round-trips, and scrolling preserve exact storage bytes, revision, resources, receipts, journals, Chronicle state, and tutorial state;
- focus return, dialog semantics, 44px targets, true vertical scrolling, bottom-navigation clearance, overflow, 130% text stress, reduced motion, and the console remain healthy.

Established and high-investment injected public fixtures retain one predecessor limitation: the schema-14 direct-origin validator refuses their fresh-save preview. The browser gate accepts only the exact fail-closed response containing `Fresh save unavailable`, `Named schema-13 and direct-origin validators must approve activation`, and `Nothing was replaced`, with focus return and complete byte neutrality.

Run:

```sh
npm ci
npx playwright install chromium
npm run qa:phase24f-more
node qa/phase-24f-more/verify.mjs
```
