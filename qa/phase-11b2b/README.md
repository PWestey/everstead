# Phase 11B-2b · Transaction engine gate

This isolated-storage gate exercises import, Previous-save restore and toggle, recoverable reset, owner-checked Forget, forensic reset, zero-write refusals, every transaction destination, interrupted writes, silent foreign interference, and pre-bootstrap journal recovery. It verifies exact 15-slot convergence, migrated schema-11 lineage, byte-exact rollback, rich Rank/economy/Family/Tower/Expedition round trips, and direct runtime adoption without using native browser storage.

The current gate contains 103 assertions. The later Phase 11B-2c gate owns the final browser UI, verification-read/quota/repeated-reload matrix, frozen predecessor manifest, and public-release evidence.

Run:

```sh
/Users/westmanfamily/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node qa/phase-11b2b/engine-probe.mjs
```
