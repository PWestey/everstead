(()=>{'use strict';const specs=[['320x568-normal',320,568,'normal'],['320x568-reduced',320,568,'reduced'],['390x844-normal',390,844,'normal'],['390x844-reduced',390,844,'reduced']],button=document.getElementById('run'),tbody=document.getElementById('rows'),fatal=document.getElementById('fatal'),realms=document.getElementById('realms');let running=false;
function render(rows){tbody.textContent='';for(const row of rows){const tr=document.createElement('tr');for(const value of [row.id,row.status,row.evidence]){const td=document.createElement('td');td.textContent=value;td.className=row.status.toLowerCase();tr.append(td)}tbody.append(tr)}const count=status=>rows.filter(row=>row.status===status).length;document.getElementById('total').textContent=rows.length;document.getElementById('passed').textContent=count('PASS');document.getElementById('pending').textContent=count('PENDING');document.getElementById('failed').textContent=count('FAIL');window.__EVERSTEAD_PHASE_10B2_RESULT__={total:rows.length,passed:count('PASS'),pending:count('PENDING'),failed:count('FAIL'),fatal:fatal.textContent,rows}}
async function run(){if(running)return;running=true;button.disabled=true;fatal.textContent='';realms.textContent='';const received=new Map(),rows=[],receive=event=>{if(event.origin!==location.origin||!event.data||!String(event.data.type).startsWith('phase10b2-realm'))return;if(event.data.type==='phase10b2-realm-fatal'){fatal.textContent=event.data.error;return}received.set(event.data.realmId,event.data)};window.addEventListener('message',receive);
for(const [id,width,height,motion]of specs){const frame=document.createElement('iframe');frame.width=width;frame.height=height;frame.sandbox='allow-scripts allow-same-origin';frame.src=`realm.html?realm=${encodeURIComponent(id)}&motion=${motion}`;realms.append(frame)}
const start=performance.now();while(received.size<4&&!fatal.textContent&&performance.now()-start<30000)await new Promise(resolve=>setTimeout(resolve,25));window.removeEventListener('message',receive);
const realmRows=[...received.values()].flatMap(value=>value.rows),realmIds=[...received.keys()],unique=new Set(realmRows.map(row=>row.id));rows.push({id:'runner-dashboard-identity',status:fatal.textContent?'FAIL':'PASS',evidence:'dashboard completed without fatal error'},{id:'runner-realms-complete',status:received.size===4?'PASS':'FAIL',evidence:`${received.size}/4 realms completed`},{id:'runner-row-registry',status:realmRows.length===224&&unique.size===224?'PASS':'FAIL',evidence:`${realmRows.length} realm rows; ${unique.size} unique`},{id:'runner-realm-identities',status:specs.every(([id])=>realmIds.includes(id))?'PASS':'FAIL',evidence:realmIds.join(', ')});rows.push(...realmRows);render(rows);button.disabled=false;running=false}
button.addEventListener('click',run);run();})();
