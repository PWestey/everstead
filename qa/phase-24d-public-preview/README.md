# Phase 24D limited public-preview gate

This successor gate defines the GitHub Pages deployment as a **Limited Public Preview** and tests the ordinary production path rather than relying on the app's QA bridges.

The browser matrix boots fresh, public-established, foundation-thin migrated, and public high-investment saves at 320×568 and 390×844. Each of the eight journey realms receives an isolated storage adapter but no `runtime.qa` authorization and no `qa=1` query. The established and high profiles are created through the real production Oath, Campaign, offline-claim, and stage-repeat controls. The high profile reaches Rank 5 and all four Adventure roads. The foundation-thin profile is prepared separately because that historical migration state cannot be recreated through present-day gameplay; its actual journey is still fully non-QA.

Every journey selects all five primary destinations, verifies all three rosters, exercises every available Adventure road, opens the critical character, Building, Oath, Codex, Chronicle, Tutorial, Legacy, and scaling surfaces, and fails on console warnings/errors, horizontal overflow, or native-storage access. Seed capture rejects private policy state, QA credit ledgers, QA/fixture lineage, and abandoned staging or save-tool journals.

Phase 24D is presentation-only. The verifier removes its title, loader, and public-preview card and requires the remaining `index.html` bytes to equal the accepted Phase 24C-2D hotfix. CI also exports that exact predecessor commit and runs its original frozen gates there before checking this successor.

Run:

```sh
npm ci
npx playwright install chromium
npm run qa:public-preview
node qa/phase-24d-public-preview/verify.mjs
```
